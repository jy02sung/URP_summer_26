<title>Dual-Arm Vision Pick & Place — PRD</title>

# Dual-Arm Vision-Guided Pick & Place

**PRD 요약 뷰** · 2026-07-08 · `Dual_Arm_ROS_Simulation` (branch `jmp`)

> 전체 서술형 상세는 저장소의 `PRD.md`를 참고. 이 문서는 파이프라인 구조, 구현 상태,
> 기능 간 의존관계, 우선순위를 한눈에 보기 위한 다이어그램 중심 요약본.

**범례** — ✅ 완료 · 🟡 부분 구현 · ❌ 미구현

---

## 1. 전체 파이프라인

사용자가 물체 좌표/특성(무게·크기)과 목표 위치를 입력하면 로봇이 아래 6단계를 자동 수행하는 것이 최종 목표다. 현재는 3·5·6단계만 완료되어 있고, 1단계는 손대지 않은 상태, 2·4단계는 절반만 동작한다.

```
INPUT : 물체 좌표/특성(무게·크기) + 목표 위치
  │
  ▼
①  Head 자동 스캔 (물체 탐색)                          ❌ 미구현
  │   11DoF Head·RGB-D 카메라는 있지만 스캔 로직이 없어
  │   물체가 고정된 카메라 시야 안에 있을 때만 인식 가능
  ▼
②  ArUco 3D 좌표 계산 (id26 물체 / id27 목표)           🟡 부분 구현
  │   물체(id26)는 자동 인식되나 버그 있음.
  │   목표(id27)는 Gazebo 모델만 있고 검출 미연동 → 현재 CLI로 수동 좌표 입력
  ▼
③  양팔 동시 접근 · 파지 (DLS IK + Quintic 궤적)        ✅ 완료
  │   6D 스택 IK, 팔별 독립 Tf, 6-세그먼트 궤적
  ▼
④  임피던스 제어로 파지력 자동 조절                      🟡 부분 구현
  │   제어기 자체는 동작하나 Md/Bd/Kd가 전역 상수로 고정
  │   → "물체 특성에 따라" 조절되는 부분이 아직 없음
  ▼
⑤  목표 위치로 이송                                     ✅ 완료
  │
  ▼
⑥  내려놓기 → 복귀                                      ✅ 완료
```

---

## 2. 기능별 구현 상태

| # | 기능 | 상태 | 핵심 내용 |
|---|------|:---:|-----------|
| 1 | Head 11DoF + RGB-D 카메라 마운트 | ✅ | yaw ±70°, pitch ±30°, `Head_camera_link_optical` 프레임 |
| 2 | **Head 자동 스캔** | ❌ | 탐색 패턴·재탐색 로직 전무. 하드웨어만 존재 |
| 3 | ArUco 물체 인식 (id=26) | 🟡 | `aruco_ros/single` 1개 인스턴스로 자동 검출, **인식 버그 미해결** |
| 4 | ArUco 목표 인식 (id=27) | ❌ | Gazebo 모델만 존재, 검출 파이프라인 미연동 → CLI 수동 xyz 입력으로 대체 중 |
| 5 | TF 변환 (카메라 → world) | ✅ | `tf::TransformListener`, TF 실패 시 궤적 생성 스킵 |
| 6 | 양팔 동시 파지 궤적 | ✅ | DLS IK(6D 스택) + 6-세그먼트 Quintic, 세그먼트별 TaskPhase 태깅 |
| 7 | 파지 오프셋 파라미터화 | ❌ | `straddle/grasp/lift offset`이 10cm 정육면체 기준 상수로 고정 |
| 8 | 임피던스 제어기 | ✅ | `Md·ë + Bd·ė + Kd·e = F_ext`, F/T 센서 피드백, 파지~내려놓기 구간에만 활성화 |
| 9 | **물체 특성 → 파라미터 자동 조절** | ❌ | 무게·크기 입력 인터페이스 자체가 없음. `Md/Bd/Kd` 전역 상수 고정 |
| 10 | TaskPhase 자동 전환 | ✅ | 접근/파지-이송/복귀 3단계, 궤적 재생 중 자동 전환 + 토픽 발행 |
| 11 | 전체 파이프라인 통합 자동화 | ❌ | 현재 트리거: CLI에서 모드 선택 + 목표 xyz 수동 입력. 원샷 자동 실행 아님 |

---

## 3. 의존관계

무엇이 무엇을 전제로 하는지. Tier 0(완료된 기반) 위에 Tier 1(진행·미구현 기능들)이 얹히고, Tier 1이 모두 갖춰져야 Tier 2(통합 자동화)가 성립한다.

```
Tier 0 · 기반 (완료)
  A   Head 11DoF + RGB-D 카메라 HW
  B   TF 카메라 → world 좌표 변환
  C   DLS IK + Quintic 궤적 엔진
  D   임피던스 제어기 (파라미터는 상수)
  E   TaskPhase 상태머신 (자동 전환)

Tier 1 · 진행 / 미구현  (Tier 0 위에 구축)
  F   ArUco 인식 버그 수정              depends on  B
  G   Head 자동 스캔                    depends on  A
  H   목표(id27) 검출 연동              depends on  F
  I   물체특성 → 파지·임피던스 자동화     depends on  C, D

Tier 2 · 통합
  J   전체 파이프라인 원샷 자동화        depends on  E, F, G, H, I
```

```
A ──▶ G
B ──▶ F ──▶ H
C ──▶ I
D ──▶ I
{E, F, G, H, I} ──▶ J
```

**읽는 법**: F(ArUco 버그 수정)가 막혀 있으면 H(목표 자동 인식)도 시작할 수 없다. G(Head 스캔)와 I(물체특성 자동화)는 서로 독립적이라 병렬로 진행 가능. J(전체 자동화)는 F·G·H·I 넷 중 하나라도 비면 완성되지 않는다.

---

## 4. 우선순위 로드맵

| 순위 | 작업 | 선행조건 | 완료 시 효과 |
|:---:|------|---------|-------------|
| P0 | ArUco 인식 버그 수정 | — | 모든 비전 하위 기능(H 포함)의 신뢰성 전제조건 |
| P1 | Head 자동 스캔 | A (완료) | 사용자가 카메라 시야를 신경 쓰지 않아도 물체 탐지 |
| P2 | 물체 특성 → 임피던스/파지 파라미터 자동화 | C, D (완료) | 무게·크기별 파지력·간격 자동 조절 — 상수 하드코딩 제거 |
| P3 | 목표지점(id=27) 자동 인식 | P0 | 목표 좌표 수동 입력(CLI) 제거 |
| P4 | 전체 파이프라인 통합 자동화 | P0–P3 전부 | "입력 1회 → 완전 자동 실행" 최종 목표 달성 |

```
P0 ─────┐
        ├──▶ P3 ─────┐
P1 ─────┼────────────┼──▶ P4
P2 ─────┘────────────┘
```

---

## 5. 관측 가능한 런타임 신호 (참고)

파이프라인 진행 상황은 아래 토픽으로 외부에서 관측 가능 — 통합 자동화(P4) 작업 시 상태머신의 훅 포인트로 재사용 가능.

| 토픽 | 타입 | 의미 |
|------|------|------|
| `/aruco_ros/pose` | `geometry_msgs/PoseStamped` | 카메라 프레임 기준 검출된 마커 pose |
| `/dual_arm/TaskPhase` | `std_msgs/Int32` | 0=접근, 1=파지~내려놓기, 2=복귀 (전환 시점에만 발행) |
| `/dual_arm/TrajectoryDone` | `std_msgs/Bool` | 궤적(복귀 포함) 실행 완료 시 1회 발행 |
| `/dual_arm/left,right_ft_sensor` | `geometry_msgs/WrenchStamped` | 임피던스 제어의 `F_ext` 입력 |
