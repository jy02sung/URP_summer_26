# 🤖 Dual-Arm Pick & Place 제어공학 마스터 가이드북
### — 임피던스/어드미턴스 제어, Coupled IK, Null-space Projection의 이론·수식·코드 완전 해설 + 15분 발표 대본

> **이 문서의 목적**: 발표자(사용자)가 이 로봇이 "왜 이렇게 동작하는지"를 수식 한 줄까지 스스로
> 설명할 수 있도록, 실제 소스코드(`dual_arm_function.cpp`, `main.cpp`, `dual_arm.xacro`)를
> 라인 단위로 분석해 이론과 매핑한 자료입니다.

> ⚠️ **먼저 바로잡는 사실 하나**: 이 문서를 준비하며 실제 개발 기록(`PROJECT_HISTORY.md`,
> `발표용.md`)을 다시 확인한 결과, "임피던스가 불안정해서 실패 → 어드미턴스로 바꿔서 해결"이라는
> 흔한 통념은 **이 프로젝트의 실제 히스토리와 다릅니다.** 임피던스 제어는 F/T 로우패스 필터
> 적용 후 **독자적으로 안정 동작에 성공**했었고(리프트 1.302m 도달, 슬립 사실상 0), 그 뒤
> **어드미턴스로 전환한 이유는 "불안정해서"가 아니라 축마다 다른 제어 법칙(x,y는 위치추종,
> 스퀴즈축은 순수 힘추종)을 섞고 싶다는 새 설계 요구사항 때문**이었습니다. 이 문서는 이 정확한
> 인과관계로 설명합니다 — 발표 중 "그럼 임피던스가 왜 실패했다는 근거가 있나요?"라는 질문에
> 흔들리지 않기 위한 가장 중요한 방어선입니다.

---

# PART 0. 전체 아키텍처 한눈에 보기

```
[Head 스캔] → [ArUco 인식] → [Coupled IK로 접근] → [접촉/파지: 어드미턴스 ON]
                                                            │
                                                            ▼
                              [Null-space projection이 매 tick 팔꿈치를
                               사람처럼 자연스러운 자세로 유도]
                                                            │
                                                            ▼
                                    [이송] → [내려놓기] → [복귀: 어드미턴스 OFF]
```

| 구성요소 | 이 로봇에서의 역할 | 이 문서의 Part |
|---|---|---|
| **어드미턴스 제어** | 파지~이송 구간(`PHASE_GRASP_TO_PLACE`)에서 힘 센서 피드백으로 스퀴즈력을 유지 | Part 1 |
| **9DOF Coupled IK** | 양팔 + 허리를 하나의 방정식으로 동시에 풀어 허리 충돌 원천 차단 | Part 2 |
| **Null-space Projection** | 손끝 위치는 그대로 두고 팔꿈치만 "사람처럼" 밖으로 유도 | Part 3 |

---
---

# PART 1. 임피던스 제어 vs 어드미턴스 제어

## 1.1 지배 방정식은 똑같이 생겼다 — 그런데 무엇이 다른가?

두 제어기 모두 아래의 **가상 스프링-댐퍼-질량 방정식**을 사용합니다:

$$M\ddot{e} + D\dot{e} + Ke = F_{ext}$$

여기서 $e = x_{actual} - x_{desired}$ (실제 위치 - 목표 위치의 오차)입니다.

> 🎓 **고등학생도 이해하는 비유**: 로봇 손목에 눈에 안 보이는 "가상의 스프링+댐퍼+질량 덩어리"가
> 달려있다고 상상하세요.
> - **$M$ (가상 질량)**: 이 덩어리가 얼마나 "묵직하게" 반응할지. 무거울수록 힘을 줘도 천천히 움직임(굼뜸).
> - **$D$ (가상 댐퍼/감쇠)**: 자동차 서스펜션 같은 것. 클수록 덜렁거리지 않고 부드럽게 멈춤(진동 억제).
> - **$K$ (가상 강성)**: 스프링의 뻣뻣함. 클수록 목표 위치에서 벗어나려 하면 강하게 되돌아옴.
>
> 물체를 만졌을 때($F_{ext}$가 생겼을 때) "이 가상 스프링-댐퍼가 어떻게 반응할지"를 수학으로 미리
> 정해두는 것 — 이게 이 방정식의 정체입니다.

**진짜 차이는 이 방정식을 어디에 꽂아 넣느냐(인과관계, causality)입니다:**

| | 임피던스 (Impedance) | 어드미턴스 (Admittance) |
|---|---|---|
| **입력 → 출력** | 위치·속도 오차 → **힘(토크)** | 힘 → **위치·속도 기준** |
| **로봇에게 필요한 것** | 토크(전류) 제어 인터페이스 | 위치/속도 인터페이스 + 그걸 다시 추종할 내부 루프 |
| **루프 구조** | **단일 루프** (힘→가속도→토크가 한 번에) | **이중 루프** (힘→위치기준 생성 루프 + 위치기준 추종 루프) |

## 1.2 이 프로젝트에서 실제로 벌어진 일 (교과서 이야기 아님)

### 1.2.1 왜 처음엔 임피던스를 선택했나 (2026-07-08, `발표용.md` §7)

이 로봇은 `ARMCTRLMODE == EFFORT`로 **이미 토크(전류) 제어 인터페이스가 있고**,
`pinocchio::rnea()`로 관절 토크를 직접 계산해 `.../effort_controller/command`로 보내고
있었습니다. 즉 "위치 제어기만 있고 토크 인터페이스가 없어서 어드미턴스를 써야 하는" 교과서적
상황이 **애초에 아니었습니다.**

- 어드미턴스는 힘 센싱(느리고 노이즈 있음) → 목표위치 생성 → 그 목표를 다시 빠르고 강성 높은
  내부 위치루프가 추종해야 하는 **이중 루프**가 되고, 두 루프의 대역폭을 잘 분리 못 하면
  불안정해지기 쉽습니다.
- 임피던스는 힘을 곧바로 가속도 보정으로 바꿔 **같은 제어 루프(1kHz 계산토크) 안에서 한 번에**
  처리 — 구조가 더 단순합니다.
- "위치가 정확히 안 알려진 강체에 다가가다 접촉하면, 그 반발력 자체가 파지력이 되어야 하는"
  이 태스크 특성과 임피던스 모델이 정확히 맞습니다.

**결과: 임피던스만으로 실제로 성공했습니다.** F/T 원시값 노이즈를 10Hz LPF로 거른 뒤
(`fc1b15f` 커밋), 리프트 최고높이 1.266m(미달)→**1.302m(목표 달성)**, 그립 중 최저높이
0.05m(자유낙하)→**1.19994m(슬립 사실상 0)**. 물체가 목표 지점 근처에 정지 상태로 안착했습니다.

### 1.2.2 왜 나중에 어드미턴스로 바꿨나 (2026-07-13, P0)

새 설계 요구사항(`PRD_dual_arm_v3.md`)이 생겼습니다: **"x, y축은 계획된 궤적을 그대로
추종(위치제어)하되, 스퀴즈 축만 목표 힘(10N)을 정확히 유지(순수 힘제어, 강성 $K=0$)하고
싶다"** — 즉 **한 몸에 서로 다른 제어 법칙을 축마다 다르게 섞어 쓰고 싶다**는 요구였습니다.

이걸 임피던스(토크 레벨) 구조에서 하려면, 관절 토크로 변환되기 직전 단계에서 축별로 완전히
다른 동역학 법칙을 정교하게 분리해 넣어야 해서 코드가 복잡해집니다. 반면 어드미턴스(궤적
레벨)는 "축마다 다른 법칙으로 위치 목표(`x_cmd`)만 다르게 계산하고, 그 결과를 **동일한 IK
함수 한 번**에 넣으면 끝"이라 훨씬 단순하게 표현됩니다. **이게 전환의 진짜 이유입니다 —
안정성 문제가 아니라 표현력(expressiveness) 문제였습니다.**

> 실제로 이 세션에서 사용자가 직접 확인: **"임피던스도 성공했고 어드민턴스도 성공했어."**

### 1.2.3 그럼 실제로 있었던 "불안정/실패" 사건들은 뭐였나?

발표에서 "그럼 왜 이것저것 실패한 기록이 많은가요?"란 질문이 나올 수 있습니다. 답: **제어
방식(임피던스 vs 어드미턴스) 자체의 문제가 아니라, 아래 4가지 구체적이고 각기 다른 원인들
때문**이었습니다.

| 실제 있었던 문제 | 원인 | 조치 |
|---|---|---|
| F/T 값이 수십N씩 수ms만에 요동, 토크로 증폭되어 접촉이 떨림(chatter) | F/T 원시값을 필터 없이 그대로 사용 | 10Hz LPF 적용 (`FT_LPF_CUTOFF_HZ`, `dual_arm_function.h:141`) |
| deltaY(순응 변위)가 K=0이라 접촉 순간유실 시 무한정 커짐(발산 확인) | 순수 힘추종 적분기는 되돌리는 힘이 없음 | 속도/변위 하드 리밋 (`Y_CMD_VEL_LIMIT`, `Y_CMD_MAX_DISP`) |
| 접촉 강성(Gazebo 기본 kp=1e12=사실상 강체)이 너무 높아 접촉 임펄스가 큼 | URDF/SDF 어디에도 `<contact>` kp/kd가 명시된 적 없어 기본값 방치 | `kp: 1e12→1e5`, `kd: 1.0→100.0` 재설정 |
| 팔꿈치가 관절 한계에 붕괴(자세한 내용은 Part 2, 3) | 태스크 차원 대비 실질 가용 관절 수 부족 (DOF 문제, 제어 법칙과 무관) | Coupled IK + Null-space (Part 2, 3) |

## 1.3 실제 구현 수식 (현재 코드, `main.cpp:730-825`)

```
1) x,z (위치추종, K≠0):
   ẍ_cmd = ẍ_d - [D(ẋ_cmd-ẋ_d) + K(x_cmd-x_d)] / M

2) y (스퀴즈, 힘추종, K=0 순수 적분기):
   ÿ_cmd(deltaY) = [F_ext,y - F_d,y - D·ẏ_cmd] / M

3) Euler 적분: ẋ_cmd += ẍ_cmd·Δt,  x_cmd += ẋ_cmd·Δt   (deltaY는 하드 리밋 포함)

4) x_cmd → SolveIK_Position() → q_cmd  (Part 2에서 상세 설명)

5) q̇_cmd = (q_cmd - q_cmd_prev)/Δt   (후진차분, 온라인이라 미래 샘플 없음)

6) PDController(q_cmd, q_actual, q̇_cmd, q̇_actual) → PD_acc

7) τ = RNEA(q, q̇, q̈_cmd + PD_acc)   ← pinocchio 실제 관성행렬 반영
```

### 코드 매핑 표

| 수식 요소 | 코드 위치 | 실제 값 |
|---|---|---|
| $M$ (가상 질량) | `dual_arm_function.h:149` `Ma_left[3] = {2.0, 2.0, 2.0}` | 2.0 kg |
| $D$ (가상 댐핑) | `dual_arm_function.h:150` `Da_left[3] = {65.0, 65.0, 65.0}` | 65.0 N·s/m |
| $K$ (x,z축 강성) | `dual_arm_function.h:151` `Ka_left[3] = {500.0, 0.0, 500.0}` | 500 N/m (y축만 0) |
| 목표 스퀴즈력 $F_d$ | `dual_arm_function.h:161-162` `ADMITTANCE_FD_Y_LEFT=10.0 / RIGHT=-10.0` | ±10N |
| F/T 로우패스 필터 | `dual_arm_function.h:141` `FT_LPF_CUTOFF_HZ=10.0` + `main.cpp:764-766` | 10Hz |
| deltaY 하드 리밋 | `dual_arm_function.h:185-186` `Y_CMD_MAX_DISP/VEL_LIMIT=0.03` | 3cm / 0.03m/s |
| x,z 위치추종 가속도 계산 | `main.cpp:777-779` `for (int k : {0,2})` 루프 | — |
| y 힘추종(deltaY) 계산 | `main.cpp:784-804` | — |
| PD + RNEA 최종 토크 | `main.cpp:818-820`, `main.cpp:899` `pinocchio::rnea(...)` | — |

---
---

# PART 2. 9자유도 Coupled IK (Stacked Jacobian)

## 2.1 문제: 왜 양팔을 따로 풀면 허리에서 충돌이 나는가

이 로봇은 왼팔·오른팔이 **하나의 공유 허리(Waist) 관절**을 base로 삼아 뻗어 나갑니다.
양팔을 **독립적으로** IK를 풀면(왼팔 IK 따로, 오른팔 IK 따로) 무슨 일이 생기는지 보겠습니다.

```
왼팔 IK 솔버: "상자를 잡으려면 허리를 왼쪽으로 틀어야 해!" → Waist에 +Δθ 명령
오른팔 IK 솔버: "상자를 잡으려면 허리를 오른쪽으로 틀어야 해!" → Waist에 -Δθ 명령
                              │
                              ▼
        같은 물리적 관절(Waist)에 반대 방향 명령이 동시에 꽂힘
                              │
                              ▼
              평균 내면 상쇄되어 정체, 혹은 진동/발산
```

**이건 이 프로젝트에서 실제로 겪은 문제입니다.** 팀원(`jys` 브랜치)이 정확히 이 구조(양팔
독립 IK)로 null-space 제어를 구현했는데, waist를 포함시키려 하자 왼팔이 원하는 waist 회전
방향(−0.029)과 오른팔이 원하는 방향(+0.029)이 정반대로 나와 평균을 내면 상쇄되며 로봇이
굳어버리는 현상이 실측으로 확인됐습니다.

## 2.2 해법: 두 팔의 야코비안을 하나로 쌓는다 (Stacked Jacobian)

**핵심 아이디어**: 왼팔 따로, 오른팔 따로 두 개의 방정식을 푸는 게 아니라, **"왼손도 목표에
가고 + 오른손도 목표에 간다"를 동시에 만족하는 단 하나의 방정식**을 세우면, 허리 관절은 그
하나의 방정식 안에서 **자동으로 유일한 최적 절충값**을 찾게 됩니다 —애초에 "충돌"이라는
개념 자체가 성립하지 않습니다 (경쟁하는 두 명령이 없으니까요).

### 수학적 유도

각 손끝(End-Effector)의 속도는 야코비안을 통해 관절속도와 연결됩니다:

$$\dot{x}_L = J_L \dot{q}, \qquad \dot{x}_R = J_R \dot{q}$$

여기서 $\dot{q} \in \mathbb{R}^9$는 **9개 관절 전부**(Waist 1 + 왼팔 4 + 오른팔 4)의 속도
벡터입니다 — $J_L$과 $J_R$은 **같은 $\dot{q}$**를 공유합니다 (Waist는 두 야코비안 모두에서
0이 아닌 컬럼을 가짐 — 두 손 모두 허리 회전에 반응하기 때문).

이 두 방정식을 세로로 쌓으면:

$$
J_{stacked} = \begin{bmatrix} J_{L} \\ J_{R} \end{bmatrix} \in \mathbb{R}^{6 \times 9},
\qquad
\dot{x}_{stacked} = \begin{bmatrix} \dot{x}_L \\ \dot{x}_R \end{bmatrix} \in \mathbb{R}^{6}
$$

> 📐 **실제 코드의 차원**: 이 로봇의 IK는 **방향(orientation)은 풀지 않고 위치(3D)만** 푸므로
> $J_L, J_R \in \mathbb{R}^{3\times9}$ (각 3행), $J_{stacked} \in \mathbb{R}^{6\times9}$
> 입니다(왼손 위치 3 + 오른손 위치 3 = 6행, 9개 관절 = 9열). "왼손 6D + 오른손 6D = 12D"가
> 아니라 **"왼손 위치3D + 오른손 위치3D = 6D"** 라는 점이 중요합니다 — 이유는 Part 3에서
> 설명합니다(방향까지 풀면 관절 수가 모자라서 null-space가 사라짐).

이제 오차(목표 - 현재)를 정의하고 **감쇠 최소자승법(DLS, Damped Least Squares)**으로 풉니다:

$$e = \begin{bmatrix} t_L - x_L \\ t_R - x_R \end{bmatrix} \in \mathbb{R}^6,
\qquad
\dot{q} = J_{stacked}^{\dagger} e,
\qquad
J^{\dagger}_{stacked} = J_{stacked}^{T}(J_{stacked}J_{stacked}^{T} + \lambda^2 I)^{-1}$$

$\lambda$(댐핑 계수)는 특이점(singularity) 근처에서 $J J^T$가 특이행렬에 가까워질 때
수치적으로 안정된 해를 주기 위한 정칙화(regularization) 항입니다 — 순수 의사역행렬
$(JJ^T)^{-1}$ 대신 $\lambda^2 I$를 더해 항상 역행렬이 존재하게 만듭니다.

**이 방정식 하나에는 "왼팔 담당", "오른팔 담당"의 구분이 없습니다.** 허리(Waist) 컬럼의
최적값은 왼손 오차와 오른손 오차를 **동시에 최소화하는 최소자승해**로 자동 결정됩니다 —
수학적으로 "충돌"이 원천적으로 발생할 수 없는 구조입니다.

### 코드 매핑

```cpp
// dual_arm_function.cpp:252-263
pinocchio::Data::Matrix6x JL(6, model.nv); JL.setZero();
pinocchio::Data::Matrix6x JR(6, model.nv); JR.setZero();
pinocchio::getFrameJacobian(model, data, l_EE, pinocchio::LOCAL_WORLD_ALIGNED, JL);
pinocchio::getFrameJacobian(model, data, r_EE, pinocchio::LOCAL_WORLD_ALIGNED, JR);

MatrixXd J(6, model.nv);
J.topRows<3>()    = JL.topRows<3>();   // 왼손 위치 3행
J.bottomRows<3>() = JR.topRows<3>();   // 오른손 위치 3행  ← 여기가 J_stacked

MatrixXd JJt = J * J.transpose() + (lambda*lambda) * MatrixXd::Identity(6,6);
MatrixXd JJt_inv = JJt.ldlt().solve(MatrixXd::Identity(6,6));
MatrixXd Jpinv = J.transpose() * JJt_inv;   // J^dagger, DoF x 6

VectorXd dq = Jpinv * e;   // e = [target_L - xL ; target_R - xR]
```
(`dual_arm_function.cpp:207-286`, 함수 `SolveIK_Position`)

| 수식 기호 | 코드 변수 | 위치 |
|---|---|---|
| $J_{stacked}$ | `J` (6×model.nv) | `dual_arm_function.cpp:257-259` |
| $\lambda$ | `lambda = 0.1` | `dual_arm_function.cpp:212` |
| $e$ | `e` (6×1) | `dual_arm_function.cpp:246-248` |
| $J^{\dagger}$ | `Jpinv` | `dual_arm_function.cpp:263` |
| $\dot{q} \cdot \Delta t$ | `dq`, `q += step*dq` | `dual_arm_function.cpp:265, 278` |

**활성 관절 9개**: Waist(1) + L arm(4: shoulder pitch/roll/yaw, elbow) + R arm(4) — Head
yaw/pitch는 Torso에서 팔과 분리된 별도 기구학 분기라 애초에 $J$에서 해당 컬럼이 0입니다
(`main.cpp` DoF 배열 주석 참고, `dual_arm_function.h:38-39`).

---
---

# PART 3. 우아한 포즈를 위한 Null-space Projection

## 3.1 여유 자유도(Redundancy)란?

**태스크의 차원**(우리가 진짜로 신경 쓰는 것 = 양손 위치 = 6D)보다 **로봇의 관절 수**(9개)가
많으면, "손끝 위치를 정확히 맞추는" 해가 **무한히 많이** 존재합니다. 이 "남는 자유도"를
여유 자유도(redundancy)라 부르고, 그 크기는:

$$\dim(\text{null-space}) = n_{joints} - \text{rank}(J) = 9 - 6 = 3$$

> 🎓 **비유**: 손끝으로 책상 위 한 점을 가리키는 자세는 사람 팔로도 무수히 많습니다(어깨를
> 돌리면서 팔꿈치를 들었다 내렸다 해도 손끝은 그 점에 그대로 있을 수 있죠). "손끝 위치"라는
> 조건 하나로는 팔 전체 자세가 하나로 정해지지 않습니다 — 이 프로젝트에서 그 남는 자유도가
> 정확히 3개라는 뜻입니다.

**pinocchio로 실측 검증**: 300~500회 랜덤 자세에서 $J_{stacked}$(6×9)의 rank를 계산한 결과
**항상 정확히 6** — 즉 null-space 차원은 항상 3으로 안정적입니다(특이점 없이).

> ⚠️ **왜 방향(orientation)까지 풀면 이 여유가 사라지는가 (매우 중요한 실패 교훈)**:
> 만약 위치(3D)뿐 아니라 방향(3D)까지 각 손에 대해 맞추려 하면 태스크 차원이
> $6\times2=12$가 되는데, 로봇의 실질 관절은 여전히 9개뿐입니다. $\text{rank}(J) \le
> \min(9, 12) = 9$이므로 $\dim(\text{null-space}) = 9 - 9 = 0$ — **여유 자유도가 완전히
> 사라집니다.** 이 프로젝트는 실제로 이 문제 때문에 관절한계회피 null-space를 5번 시도해서
> 5번 다 실패했었습니다(pinocchio rank 계산으로 최종 확정, `PROJECT_HISTORY.md` §3.4).
> **그래서 지금 코드는 방향을 아예 풀지 않고 위치(6D)만 풉니다** — Part 2에서 설명한 그
> "6D 스택"이 우연이 아니라, 이 null-space 여유를 확보하기 위한 의도적 설계입니다.

## 3.2 의사역행렬과 투영 행렬의 수학적 의미

DLS 해 $\dot{q} = J^{\dagger} \dot{x}$ 는 **태스크를 만족하는 무수히 많은 해 중 "관절
속도의 크기가 가장 작은" 하나**를 고릅니다(최소 노름 해). 여기에 **null-space 안에서만
움직이는 항**을 더해도 손끝 속도 $\dot{x} = J\dot{q}$ 는 전혀 안 바뀝니다 — 왜냐하면:

$$J \cdot (I - J^{\dagger}J) = J - JJ^{\dagger}J = J - J = 0$$

즉 $(I - J^{\dagger}J)$는 **"어떤 벡터를 넣어도 $J$를 곱하면 0이 되는 방향으로만 걸러주는
투영 행렬(projection matrix)"**입니다 — 기하학적으로는 "손끝을 전혀 안 흔드는 방향들의
부분공간"으로 임의의 벡터를 정사영(orthogonal projection)하는 연산자입니다.

**여기에 "팔꿈치를 이런 자세로 두고 싶다"는 선호값 $q_{pref}$를 넣으면:**

$$\dot{q} = J^{\dagger}\dot{x} + (I - J^{\dagger}J) \cdot k \cdot (q_{pref} - q)$$

- 1항($J^{\dagger}\dot{x}$): "손끝을 목표로 보내라" (태스크 명령)
- 2항($(I-J^{\dagger}J)k(q_{pref}-q)$): "남는 자유도 안에서만, 되도록 $q_{pref}$ 쪽으로
  가라" (자세 선호)

2항이 아무리 커도 $J$를 곱하면 0이 되므로(위 증명), **손끝은 1mm도 안 흔들립니다** — 이게
"손끝 위치는 그대로 두고 팔꿈치만 사람처럼 밖으로 보낼 수 있는" 이유의 전체 수학적 근거입니다.

## 3.3 이 프로젝트의 실제 구현과 $q_{pref}$

```cpp
// dual_arm_function.cpp:233-235
VectorXd q_pref = VectorXd::Zero(DoF);
q_pref(3) = 0.80;  q_pref(4) =  0.55; q_pref(5) = -0.20; q_pref(6)  = -0.45;  // L arm
q_pref(7) = 0.80;  q_pref(8) = -0.55; q_pref(9) =  0.20; q_pref(10) = -0.45;  // R arm
```
- `q_pref(4)=+0.55`(왼쪽 shoulder_roll), `q_pref(8)=-0.55`(오른쪽) → **팔꿈치를 몸통
  바깥쪽으로 벌리기(Elbow-out)**
- `q_pref(6)=q_pref(10)=-0.45`(양쪽 elbow) → **팔꿈치를 아래로 굽히기(Elbow-down)**
  (부호는 `retry-from-88pct` 브랜치에서 이미 400개 무작위 시드 탐색 + 실제 Gazebo
  검증으로 확정한 값(`L_elbow=-0.496`)과 방향이 일치함을 재확인한 뒤 사용)

```cpp
// dual_arm_function.cpp:272-276
MatrixXd N = MatrixXd::Identity(model.nv, model.nv) - Jpinv * J;   // (I - J†J)
VectorXd postureErr = posture_gain * (q_pref - q);                 // k(q_pref - q)
postureErr(1) = 0.0;  // Head_yaw는 애초 관여 안 하므로 명시적으로 제외
postureErr(2) = 0.0;  // Head_pitch도 동일
dq += N * postureErr;                                               // 최종 dq에 합산
```

| 수식 기호 | 코드 변수/값 | 위치 |
|---|---|---|
| $J^{\dagger}$ | `Jpinv` | `dual_arm_function.cpp:263` |
| $I - J^{\dagger}J$ | `N` | `dual_arm_function.cpp:272` |
| $k$ (posture gain) | `posture_gain = 0.1` | `dual_arm_function.cpp:223` |
| $q_{pref}$ | `q_pref` | `dual_arm_function.cpp:233-235` |
| 최종 $\dot{q}$ | `dq` (task항 + null항 합산) | `dual_arm_function.cpp:265, 276` |

### 왜 $k=0.35$가 아니라 $0.1$인가 (실험적 트레이드오프)

$k$(posture_gain)를 0.02~0.35로 스윕한 결과, **관절 마진은 게인과 거의 무관하게 항상
0.16~0.34rad로 유지**됐지만(9개 관절 중 null-space 3개 여유가 충분해서), 손끝 위치 잔차는
$k$가 클수록 커졌습니다($k=0.35$일 때 2.1cm, $k=0.1$일 때 6.5mm). 이건 DLS의 댐핑 항
($\lambda^2 I$) 때문에 $J^{\dagger}$가 **완벽한 직교 투영이 아니라서** 생기는 부작용입니다
— null-space 항이 이론적으로는 태스크에 전혀 영향을 안 줘야 하지만, 댐핑 때문에 아주
조금 "새어 들어갑니다". 그래서 **자세 선호는 충분히 반영되면서 정밀도는 최대한 지키는
절충값 0.1**을 최종 채택했습니다.

## 3.4 왜 이게 이 로봇에서만 특별히 어려웠나 — Waist 공유 문제와의 결합

Part 2에서 설명한 "허리 공유 관절 충돌"이 여기서 다시 등장합니다. 다른 팀원(`jys` 브랜치)이
**양팔을 독립적으로 푸는 구조**(Waist 고정, 팔당 4관절만 사용) 위에서 이 null-space
q_pref를 먼저 시도했는데:

$$\text{태스크 6D} - \text{실질 관절 4개} \Rightarrow \text{rank}(J)=4,\ \dim(\text{null-space})=4-4=0$$

**여유가 정확히 0이라 $q_{pref}$가 전혀 반영되지 않았습니다** (pinocchio로 500회 랜덤자세
검증, rank=4/4 전부 동일). Waist를 추가하면 여유가 1개 생기긴 하지만, 이번엔 Part 2에서
설명한 "왼팔이 원하는 waist 방향 ≠ 오른팔이 원하는 waist 방향" 충돌이 나타납니다.

**해법**: 이미 Coupled(Stacked Jacobian, Part 2)로 구현돼 있던 `admittance-control` 브랜치
위에 이 $q_{pref}$/null-space 로직을 이식했습니다. Coupled 구조는 Waist가 "공유 변수"가
아니라 애초에 $J_{stacked}$의 한 컬럼으로 녹아있어서 — 9개 관절 - 6D 태스크 = **여유 3**이
안정적으로 확보되고, 두 팔이 waist를 두고 경쟁하는 상황 자체가 성립하지 않습니다.

$$\underbrace{\text{Part 2 (Coupled IK)}}_{\text{허리 충돌을 원천 차단}} + \underbrace{\text{Part 3 (Null-space)}}_{\text{남는 여유로 자세 유도}} = \text{현재 완성된 시스템}$$

---
---

# PART 4. 15분 발표 스크립트 (슬라이드 페이싱)

> 총 15분 기준. 슬라이드당 예상 소요시간을 표기했습니다. 실제 발표에선 숫자를 외우려 하지
> 말고 "이 숫자가 왜 이 값인지"를 한 문장으로 설명할 수 있으면 충분합니다.

## 슬라이드 1 — 타이틀 & 데모 (1분)
**메시지**: "11자유도 양팔 로봇이 눈으로 물체를 찾아 자연스러운 자세로 잡아 옮깁니다."
- (가능하면) Gazebo 데모 영상/GIF 재생
- 한 문장 요약: "Vision → Coupled IK → 어드미턴스 파지 → Null-space로 인간형 자세"

## 슬라이드 2 — 전체 파이프라인 (1.5분)
**메시지**: 5단계 파이프라인을 한 장으로.
```
Head 스캔 → ArUco 인식 → 접근(Coupled IK) → 파지(어드미턴스+Null-space) → 이송 → 복귀
```
- "11DoF = Waist 1 + Head 2 + 양팔 4×2"
- "IK는 위치(6D)만 풀고, Null-space로 남는 자유도에서 자세를 다듬습니다" (Part 2, 3 예고)

## 슬라이드 3 — 문제의식: 두 가지 실전 난제 (1.5분)
**메시지**: "물리 시뮬레이션에서 실제로 부딪힌 두 가지 벽이 있었습니다."
1. 접촉 순간 물체가 미끄러지거나 회전하며 낙하 → **어드미턴스 파지력 제어**로 해결
2. 양팔이 허리를 두고 서로 다른 방향으로 명령 → **Coupled IK + Null-space**로 해결

## 슬라이드 4 — 임피던스 vs 어드미턴스: 지배 방정식 (2분)
**메시지**: $M\ddot{e}+D\dot{e}+Ke=F_{ext}$ 하나로 두 제어기를 다 설명.
- 고등학생 비유(질량/댐핑/강성 = 무게감/부드러움/뻣뻣함) 먼저
- "인과관계가 반대" — 임피던스(오차→힘), 어드미턴스(힘→위치기준) 표로 제시
- **"임피던스도 성공했습니다. 어드미턴스로 바꾼 건 안정성 문제가 아니라, 축마다 다른 제어
  법칙(위치추종 vs 순수 힘추종)을 섞고 싶다는 요구사항 때문이었습니다."** ← 이 한 문장이
  이 슬라이드의 핵심 방어선

## 슬라이드 5 — 어드미턴스 실제 구현 (1.5분)
**메시지**: 코드 한 블록을 그대로 보여주며 수식과 1:1 매칭.
- x,z: 위치추종 / y(스퀴즈): 순수 힘추종(K=0) + deltaY 하드리밋
- F/T 10Hz LPF, 목표 스퀴즈력 ±10N (`ADMITTANCE_FD_Y_LEFT/RIGHT`)
- "결과: 관절한계 문제가 없는 조건에서 슬립 없이 안정적으로 이송"

## 슬라이드 6 — 허리 충돌 문제 (1.5분)
**메시지**: 다이어그램으로 "왜 독립 IK가 위험한가" 시각화.
- "왼팔은 허리를 왼쪽으로, 오른팔은 오른쪽으로 — 같은 관절에 상반된 명령"
- 실측 수치 제시: 왼팔 -0.029 vs 오른팔 +0.029 (평균 내면 상쇄)

## 슬라이드 7 — Coupled IK (Stacked Jacobian) 수식 (2분)
**메시지**: $J_{stacked}=\begin{bmatrix}J_L\\J_R\end{bmatrix}$ 유도 과정을 칠판 설명하듯.
- "두 방정식을 하나로 쌓으면 허리는 '경쟁'이 아니라 '동시에 만족시키는 하나의 최적해'가 됨"
- $J(I-J^\dagger J)=0$ 증명은 생략하고 결과만: "충돌이 수학적으로 원천 차단"

## 슬라이드 8 — Null-space Projection: 여유 자유도 (2분)
**메시지**: "9개 관절로 6D 태스크 → 3개는 공짜로 남는다"
- 사람 팔 비유(손끝 고정한 채 팔꿈치만 움직이기)
- $\dot{q}=J^\dagger\dot{x}+(I-J^\dagger J)k(q_{pref}-q)$ 수식 제시
- "왜 방향까지 풀면 이 여유가 사라지는지" — 5전 5패 실패 스토리를 20초로 압축

## 슬라이드 9 — 결과 & 정량 지표 (1.5분)
**메시지**: 숫자로 마무리.
- 손끝 위치 오차: 6.5mm (grasp_offset 설계 마진 10mm 이내)
- 관절 마진: 항상 0.16~0.34rad 유지 (예전엔 0으로 붕괴)
- 그립 패드-손목 탄젠트 밀착, 물체 옆면과 평행 정렬 (dot=1.0 검증)

## 슬라이드 10 — 마무리 & Q&A 유도 (0.5분)
**메시지**: "수학(Coupled IK, Null-space)과 물리(접촉 강성, 마찰)를 함께 맞췄을 때만 실전에서 동작했습니다."

---
---

# PART 5. 예상 질문 & 모범 답안

## Q1. "왜 로봇이 잡으려 할 때 물체가 튕겨 나갔나요? 이를 제어공학적으로 어떻게 해결했습니까?"

**모범 답안**:
> "튕겨나간 원인은 하나가 아니라 세 가지였습니다. 첫째, F/T 센서 원시값이 접촉 순간 수십N
> 단위로 수 밀리초 만에 요동치는데 이걸 필터 없이 그대로 힘 제어 입력으로 썼더니 노이즈가
> 그대로 토크로 증폭됐습니다 — 10Hz 로우패스 필터로 해결했습니다. 둘째, Gazebo의 접촉 강성
> 기본값이 사실상 강체 수준(kp=10¹²)이라 접촉 순간 반발 임펄스가 컸습니다 — 저희가 URDF에
> 명시적으로 kp/kd를 낮게 재설정했습니다. 셋째는 근본적인 문제인데, 태스크 차원 대비 관절
> 자유도가 부족해서 팔꿈치가 관절 한계에 부딪혀 자세가 붕괴하는 문제였습니다 — 이건 힘
> 제어와 무관한 기구학적 문제라서, Coupled IK와 Null-space projection으로 별도로
> 해결했습니다."

## Q2. "임피던스와 어드미턴스의 수학적 차이가 무엇이며, 왜 우리 로봇에는 어드미턴스가 적합했습니까?"

**모범 답안**:
> "두 제어기 모두 $M\ddot e+D\dot e+Ke=F_{ext}$라는 같은 가상 임피던스 방정식을 쓰지만,
> 인과관계가 반대입니다. 임피던스는 위치 오차를 힘(토크)으로 직접 변환해 하나의 제어 루프
> 안에서 처리하고, 어드미턴스는 측정한 힘을 적분해 위치 기준을 만들고 그걸 별도의 위치
> 추종 루프가 따라가는 이중 루프 구조입니다.
>
> 사실 저희는 둘 다 성공적으로 구현했습니다. 임피던스가 불안정해서 어드미턴스로 바꾼 게
> 아니라, x,y축은 계획된 궤적을 그대로 따르고 스퀴즈 축만 순수 힘 제어(강성 0)로 하고
> 싶다는 요구사항 때문이었습니다. 이렇게 축마다 다른 제어 법칙을 섞는 걸 어드미턴스(궤적
> 레벨 개입)로 구현하면, 그냥 위치 목표만 축마다 다른 법칙으로 계산해서 동일한 IK 함수에
> 넣으면 되니까 구조가 훨씬 단순해집니다."

## Q3. "Null-space projection이 제대로 동작하는지 수학적/실험적으로 어떻게 증명합니까?"

**모범 답안**:
> "수학적으로는 $J(I-J^\dagger J)=JJ^\dagger J-JJ^\dagger J=0$ 이 항상 성립한다는 사실
> 하나로 증명됩니다 — null-space 투영 항에 무슨 값을 넣어도 자코비안을 곱하면 0이 되니까,
> 손끝 속도는 이론적으로 전혀 영향을 안 받습니다.
>
> 실험적으로는 두 가지를 확인했습니다. 첫째, pinocchio로 300~500회 랜덤 자세에서 스택
> 자코비안의 rank를 직접 계산해 항상 6임을 확인했습니다 — 9개 관절에서 여유가 항상 3으로
> 안정적이라는 뜻입니다. 둘째, 실제 Gazebo에서 posture gain을 0.02부터 0.35까지 스윕하며
> 손끝 위치 오차와 관절 마진을 동시에 측정했는데, 게인이 커질수록 위치 오차는 늘지만(감쇠
> 의사역행렬이 완벽한 직교투영이 아니라서 생기는 2차 효과), 관절 마진은 게인과 거의
> 무관하게 항상 확보됐습니다 — 이론과 실측이 일치했습니다."

## Q4. (추가 예상) "왜 IK에서 손의 방향(orientation)은 안 풀었나요? 그러면 손이 아무 각도로나 접촉하지 않나요?"

**모범 답안**:
> "맞습니다, 손 방향은 IK가 직접 보장하지 않습니다. 대신 그립 패드 자체를 실제 IK가 도달하는
> 손 자세를 오프라인으로 미리 계산해서, 그 자세에서 패드 법선이 물체 옆면과 정확히 평행
> (내적=1.0)해지도록 URDF에 패드 장착 각도를 하드코딩했습니다. 방향을 IK 태스크에 넣지
> 않은 이유는 태스크 차원이 늘면(6D→12D) 9개 관절로는 여유 자유도가 0이 되어 null-space
> 제어 자체가 불가능해지기 때문입니다 — 실제로 5번 시도해서 5번 다 실패한 뒤에 확정한
> 설계 결정입니다."

## Q5. (추가 예상) "Damping 계수 λ는 어떻게 정했나요? 너무 크거나 작으면 어떻게 되나요?"

**모범 답안**:
> "λ=0.1로 고정값을 씁니다. λ가 너무 작으면 특이점(singularity) 근처에서 $(JJ^T)^{-1}$이
> 발산해 관절 속도가 폭주할 수 있고, 너무 크면 감쇠가 과해서 수렴이 느려지고 태스크 오차가
> 잘 안 줄어듭니다. 이 로봇은 특이점 회피 로직이 따로 없기 때문에 λ가 이 균형을 잡아주는
> 유일한 안전장치입니다."

## Q6. (추가 예상) "실물 로봇에도 그대로 적용 가능한가요?"

**모범 답안**:
> "제어 구조(Coupled IK, Null-space, 어드미턴스)는 그대로 이식 가능합니다. 다만 접촉
> 강성/댐핑(kp/kd)은 저희가 Gazebo 물리엔진 기본값을 낮춰 튜닝한 시뮬레이션 전용 파라미터라
> 실제 로봇에서는 다시 튜닝해야 하고, F/T 센서 노이즈 특성도 실제 센서 스펙에 맞춰 로우패스
> 컷오프 주파수를 재검증해야 합니다."

---

## 부록: 핵심 상수 요약표 (발표 중 즉답용)

| 파라미터 | 값 | 코드 위치 |
|---|---|---|
| 활성 관절 수 | 9 (Waist 1 + 양팔 4×2) | `dual_arm_function.h:38` `DoF=11` (Head 2개 제외) |
| Null-space 여유 | 3 = 9 - rank(6) | pinocchio 랜덤자세 rank 검증 |
| 스택 자코비안 크기 | 6×9 (위치만, 방향 제외) | `dual_arm_function.cpp:257-259` |
| DLS 댐핑 λ | 0.1 | `dual_arm_function.cpp:212` |
| Posture gain $k$ | 0.1 (0.35→0.1로 낮춤) | `dual_arm_function.cpp:223` |
| 가상 질량 $M$ | 2.0 kg | `dual_arm_function.h:149` |
| 가상 댐핑 $D$ | 65.0 N·s/m | `dual_arm_function.h:150` |
| 가상 강성 $K$ (x,z) | 500 N/m | `dual_arm_function.h:151` |
| 목표 스퀴즈력 $F_d$ | ±10N | `dual_arm_function.h:161-162` |
| F/T LPF 컷오프 | 10Hz | `dual_arm_function.h:141` |
| 접촉 강성 kp | 1e5 (기본 1e12에서 낮춤) | `dual_arm.xacro:962, 971` |
| 접촉 댐핑 kd | 100.0 (기본 1.0에서 올림) | `dual_arm.xacro:963, 972` |
| 최종 손끝 위치 오차 | 6.5mm | 오프라인 pinocchio 검증 |
