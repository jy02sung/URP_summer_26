# Dual-Arm Vision Pick & Place — 전체 개발 히스토리 통합 기록

> 여기저기 흩어져 있던 기록(각 워크스페이스의 md 파일, `~/Downloads`의 PRD/보고서, Notion
> 개발일지, git 커밋 로그)을 하나로 모은 문서. 시간순으로 정리했고, 각 단계마다 실패한 시도도
> 성공한 시도와 동일한 비중으로 남겼다 — 특히 "임피던스 제어 구현"과 "임피던스→어드미턴스
> 전환 과정의 실패/성공"이 이 문서의 핵심 축이다.
>
> **로봇 구성**: 11DoF (Waist 1 + Head yaw/pitch 2 + 양팔 4×2: shoulder pitch/roll/yaw, elbow).
> Head는 Torso에서 팔과 분리된 별도 분기라 IK에 관여하지 않는다. 손목(wrist) 관절은 이
> "admittance-control" 계열 URDF엔 아예 없다(다른 실험 브랜치에서만 존재).
>
> **출처**: `friend_ws`(retry-from-88pct)의 `PRD.md`/`IMPEDANCE_GRASP.md`/`발표용.md`/
> `RETRY_FROM_88PCT.md`/`SESSION_20260716_wrist_and_orientation_ik.md`, `~/Downloads`의
> `PRD_dual_arm_v2/v3.md`, `dual_arm_ik_tuning_report-v3.md`, `기록.md`(Notion 사본),
> Notion 페이지 "Dual-Arm PRD v3 개발 일지", `friend_ws`/`friend_ws_88pct`/`friend_ws_jys`
> 세 워크스페이스의 git 커밋 로그.

---

## 0. 최종 상태 요약 (먼저 결론부터)

**지금(2026-07-17, `admittance-control` 브랜치) 성공적으로 동작하는 것:**
- Head 자동 스캔 → ArUco 인식 → world 좌표 변환
- 양팔 6D(위치) coupled DLS IK + null-space q_pref(elbow-down+out) 투영 — 관절한계 붕괴 없이 자연스러운 인간형 자세
- 원형 그립 패드(disc) — 손목 구체와 기하학적 탄젠트 접점에 밀착, 물체 옆면과 평행 정렬
- 접촉 강성/댐핑(kp/kd) 튜닝으로 접촉 임펄스 완화
- 5세그먼트 파지 파이프라인(standoff → 접촉 → lift → transport → return), 어드미턴스 스퀴즈 제어

**아직 미해결/보류:**
- F/T 실측상 목표 스퀴즈력(10N) 대비 22~42%만 전달되는 문제 — 마찰계수는 이론상 12배 여유가 있어 원인이 아님, 힘 전달/접촉 유지 메커니즘 쪽 추가 조사 필요
- ArUco 인식 버그(원인 미특정, `PRD.md` 최초 지적 이후 재검증 안 됨)
- 물체 무게/크기 → 파지 오프셋/임피던스 게인 자동 매핑 미구현 (현재 전부 상수 하드코딩)
- P3(ArUco 없는 인식), P4(테이블 신규 구축) — 착수 전

---

## 1. 초기 구현 (2026-07-08 전후) — Vision Pipeline + 임피던스 제어

### 1.1 최초 PRD와 시스템 개요

`PRD.md`(2026-07-08 작성, `jmp` 브랜치)가 최초 설계 문서. 이 시점 목표:

```
Head 자동 스캔 → ArUco 인식 → 양팔 동시 파지 → 임피던스로 파지력 자동조절 → 이송 → 복귀
```

핵심 스펙(이후로도 거의 안 바뀐 것들):
- **11DoF**: `0:Waist, 1:Head_yaw, 2:Head_pitch, 3~6:L arm, 7~10:R arm`
- **제어 모드**: Effort control 기반 RNEA 토크 제어 (`ARMCTRLMODE=EFFORT`)
- **명령 인터페이스**: `dual_arm_command` CLI → `/dual_arm/DualArmCmd_sim` → `main.cpp`가 `command_mode`별 분기(0:modeling, 1:joint sim, 2:cartesian sim, 3:vision pick)
- **IK**: DLS(Damped Least Squares), 좌우 EE를 **6D 스택 태스크로 동시 해석**(coupled, waist 포함) — λ=0.1, tol=1e-4m, maxIter=300

이 시점 기능 현황(🟡=부분구현):
| Feature | 상태 |
|---|---|
| Head 스캔 & 인식 | 🟡 하드웨어만 완료, 자동 스캔 로직 없음(고정 자세로만 인식) |
| ArUco 3D 좌표 계산 | 🟡 물체 마커는 동작, 목표지점 마커(id=27)는 미연동, 인식 버그 원인 미특정 |
| 양팔 동시 파지 궤적 | ✅ 완료 (물체 크기 고정 가정) |
| 임피던스 제어 | 🟡 제어기 자체는 동작, "물체 특성 기반 자동 조절"은 미구현(상수 고정) |
| TaskPhase 자동 전환 | 🟡 세그먼트 자동 재생까지는 완료, 처음부터 끝까지 완전 자동은 아님 |

### 1.2 Kinematic Attach 제거 → 실물리 임피던스 파지 (`IMPEDANCE_GRASP.md`, 2026-07-08)

**배경**: 원래 `aruco_box_26`이 `static=true`라서 물리적으로 손에 안 붙었고, `main.cpp`가 파지~내려놓기 구간 동안 `/gazebo/set_model_state`로 박스 pose를 손 위치에 강제로 갱신하는 "kinematic attach"(텔레포트)로 대신하고 있었다.

**조치**:
1. `model.sdf`: `static=true` → `false` (진짜 동역학 물체로 전환)
2. `main.cpp`: attach 관련 코드 전부 제거 — 이제 이미 구현돼 있던 임피던스 루프(가상 스프링-댐퍼 Md/Bd/Kd + F/T 센서)가 물체를 붙잡는 **유일한** 메커니즘이 됨
3. 원리: 양팔이 `grasp_offset=4.5cm`로 박스 표면(half-width 5cm) 안쪽 5mm를 파고드는 목표 위치를 계속 추종하려 하고, 실제 접촉이 막아서 생기는 위치오차 e에 `Kd_imp·e`가 스퀴즈력이 되어 마찰(mu=1.2)로 물체를 붙잡는다.

**부작용 처리**:
- 박스가 동역학 물체가 되며 자유낙하 → `pick_pedestal`(12x12x10cm) 추가해 파지 전까지만 지지 (예전 테이블은 팔 충돌 문제로 이미 제거된 상태였음, 전체를 가로지르지 않는 작은 받침대로 재발 방지)
- 받침대 충돌 회피를 위해 `standoffL/R` 경유점 도입, 접근을 2단계(대기지점 이동 → 파지 높이 유지한 채 y축 직선 접근)로 분리
- 파지 직후 대각선 이동 대신 수직 리프트(`LIFT_HEIGHT=0.10m`) 먼저 → 이송, 5세그먼트 파이프라인 확정(이후 세션 내내 거의 그대로 유지됨)

### 1.3 임피던스 제어 수식 및 "왜 임피던스인가"

`발표용.md` 7번 항목에 상세 정리된 근거:

| | 임피던스 | 어드미턴스 |
|---|---|---|
| 인과관계 | 위치/속도 오차 → 힘(토크) | 힘 → 위치/속도 기준 |
| 필요 인터페이스 | 토크(전류) 제어 | 위치/속도 제어 + 내부 위치루프 |

**당시 임피던스를 선택한 이유**: (1) 이미 `ARMCTRLMODE=EFFORT`로 토크 제어 인터페이스가 있고 `pinocchio::rnea()`로 토크를 직접 계산하고 있었음 — 어드미턴스처럼 "힘→위치기준→다시 위치제어" 이중 루프를 거칠 필요가 없었음. (2) 힘 센싱(느림/노이즈) → 목표위치 생성 → 내부 위치루프 추종의 이중 루프는 대역폭 분리를 잘못하면 불안정해지기 쉬움. (3) "위치가 정확히 안 알려진 강체에 접근→접촉 반발력 자체가 파지력이 되어야 하는" 이 태스크 특성이 임피던스 모델과 정확히 맞음.

**구현 수식** (`main.cpp:775-829` 당시 기준):
```
1) 가상 임피던스: Md·ë + Bd·ė + Kd_imp·e = F_ext   (e = x_actual - x_desired, world frame, 좌우 독립)
   → ë = (F_ext - Bd·ė - Kd_imp·e) / Md
2) task→joint: δq̈ = J⁺(λ)·ë,  J⁺(λ)=Jᵀ(JJᵀ+λ²I)⁻¹  (λ=IMPEDANCE_DLS_LAMBDA=0.05)
3) q̈_cmd = q̈_traj(피드포워드) + [Kp(q_d-q)+Kd(q̇_d-q̇)] + δq̈  (δq̈는 GRASP_TO_PLACE 구간만)
4) τ = RNEA(q, q̇, q̈_cmd)  ← pinocchio 실제 관성행렬 반영, 최종 토크
```
설계 단순화: 좌우 팔 결합 진짜 operational space 관성행렬 Λ(x) 대신 Md/Bd/Kd를 축별 스칼라로 단순화 — 이 때문에 자세(Jacobian)가 달라지면 같은 가상 게인이라도 실제 반응이 달라짐(후에 좌우 F/T 비대칭 원인 중 하나로 재확인됨).

### 1.4 F/T 비대칭 슬립 원인분석/수정 (`발표용.md` 5번, 커밋 `fc1b15f`)

**증상**: 파지 후 리프트~이송 도중 물체가 완전히 손에서 빠져나가 바닥 낙하.

**원인 4가지** (F/T + 박스 pose 1kHz 로깅으로 확인):
1. 파지 시작 순간부터 좌우 F/T 비대칭 (L=(-4.2,14.2,39.1N) vs R=(-0.1,-0.7,-1.5N) — 한쪽만 순간적으로 세게 파고듦)
2. **F/T 원시값이 필터 없이 그대로 임피던스 F_ext로 사용됨** — 관절속도는 LowPassFilter를 거치는데 F/T만 예외적으로 원시값 사용, 접촉 노이즈(수십N이 수ms만에 요동)가 그대로 토크로 증폭돼 접촉이 떨림(chatter)
3. 스퀴즈력이 정적 유지분만 계산됨 (grasp_offset=4.5mm×Kd_imp=300N/m≈1.5N/arm, 관성부하 미고려 — 슬립이 정확히 이송 수평속도 최고조 구간에서 시작)
4. 슬립 감지/보정 피드백 없음

**수정**: (1) F/T에 10Hz LowPassFilter 적용, (2) Kd_imp 300→500N/m, (3) grasp_offset 4.5mm→10mm 침투, (4) Bd 50→65(임계감쇠 유지)

**결과**: 리프트 최고높이 1.266m(미달)→1.302m(달성), 그립 중 최저높이 0.05m(낙하)→1.19994m(슬립 거의 0), 최종 목표 근접 정지 안착.

**추가 수정(5-1)**: 자동화 테스트는 성공했지만 사용자 직접 실행 시 접촉 순간 충격으로 회전하며 이탈하는 별도 증상 발견 → `standoffL/R→objL/R`(실제 접촉 구간)만 `v_des` 0.1→0.03m/s로 감속(다른 구간은 그대로라 전체 시간 거의 안 늘어남). 사용자 재실행으로 성공 확인.

### 1.5 그립 패드+턱 최초 추가

손 mesh가 거의 구형(반경~3.25cm)이라 점접촉만 발생 → 스퀴즈 방향 로컬면에 4×4×1cm 평면 패드 + 1cm L자형 턱 추가(마찰뿐 아니라 기하학적으로도 지지). 이 시점 좌우 패드 정렬은 dot~0.77(L)/0.99(R)로 이미 비대칭이 알려져 있었음(→ 나중에 여러 번 재조정의 씨앗이 됨).

---

## 2. 임피던스 → 어드미턴스 전환 (2026-07-13, P0) — 성공까지의 시행착오

### 2.1 전환 배경 (`PRD_dual_arm_v3.md`)

요구사항 재정의: x,y는 위치추종 어드미턴스, z(스퀴즈 방향)는 힘추종 어드미턴스(K_z=0, 목표 10N). 임피던스가 "토크 레벨" 개입이었다면 어드미턴스는 "궤적 레벨" 개입 — 오차/외력→명령가속도→적분→x_cmd(위치)→**매 tick IK를 다시 풀어 q_cmd 생성**이라는 근본적으로 다른 구조.

### 2.2 1차 구현 (커밋 `86122fb`) — Z축 스퀴즈로 구현, 검증 전

PRD 사양대로 z를 스퀴즈 축으로 구현. 사전계획 궤적(x_d,ẋ_d,ẍ_d) 보존 로직 추가, 온라인 계산이라 후진차분 사용. 빌드는 성공했으나 Gazebo 검증 전 상태로 다음 커밋으로 이어짐.

### 2.3 스퀴즈 축 Z→Y 정정 (커밋 `6fe79b9`) — 근본 설계 오류 발견

**문제**: 이 로봇은 `objL/R = obj ± (0,grasp_offset,0)`로 **world Y축** 양쪽에서 마주보고 조이는 구조인데 PRD는 Z축을 스퀴즈로 가정했음. 게다가 이송목표가 pick 지점 대비 주로 Y방향(~0.55m)으로 떨어져 있어 스퀴즈 축과 이송 축이 겹침 — "스퀴즈 축 ⊥ 이송 방향"이라는 PRD의 암묵 전제가 이 로봇엔 안 맞았음.

**시행착오**:
- 1차(Y 순수 힘추종, 기준궤적 없음): 스퀴즈 축이 곧 이송 방향이라 이송 위치성분이 없어 물체가 거의 안 옮겨짐 → 실패
- 2차: `y_cmd = y_d(t)(계획된 이송경로) + deltaY(힘오차 기반 순응변위)`로 분리. 리밋 없이는 K=0이라 접촉 순간유실 시 발산(낙하) → 속도(0.03m/s)/변위 하드리밋 추가
- 3차: 리밋 15mm는 스퀴즈력 부족(fy가 목표 10N 못 미침) → 30mm로 확대

이 커밋 단독으로는 admittance 진입 직후 좌우 접촉 비대칭으로 물체가 회전하며 거의 못 움직임 — 원인은 다음 항목(패드 정렬)으로 밝혀짐.

### 2.4 그립 패드 정렬 재계산 → "admittance-88pct" 달성 (커밋 `084d671`)

**문제**: F/T 로그에서 같은 tick에도 좌우 힘이 크게 비대칭(R -27N vs L +7.5N), 사용자가 Gazebo에서 "손 모양이 이상해서 같은 힘을 못 준다" 직접 지적.

**조치**: 패드 origin xyz는 유지, rpy(방향)만 fk_debug.cpp(일회성 도구)로 실제 SolveIK_Position을 재현해 재계산. L: dot 0.77 그대로 재확인(→ 왼팔이 더 나쁨), R: dot 0.99라던 기존 기록이 실은 stale이었고 재계산하니 0.955.

**결과**: 수정 전 admittance 진입 직후 수초 내 회전·낙하(z 1.2→0.05m) → 수정 후 물체가 목표의 **88% 지점까지 안정 도달**. 이 커밋이 이후 "admittance-88pct" 태그로 계속 참조되는 기준점이 됨. 단, 이송 막판(65.5~66.0s)에 L손 F/T가 순간 폭발(fx=18/fy=33/fz=42N)하며 회전+낙하하는 잔여 문제 있었음.

### 2.5 팔꿈치 관절한계가 근본원인 — 5회 시도 전부 실패 (커밋 `161bafa`~`dabd70b`)

**진단**: IK saturation + 세그먼트별 수렴 진단 로그 추가 후 Gazebo 재현 → 모든 문제 세그먼트에서 **L_elbow/R_elbow가 상한 1.05rad에 margin=0으로 클램프**됨을 확인. Admittance 힘 법칙 튜닝으로는 근본 해결 불가능한, 팔꿈치 관절한계/초기자세 문제였음이 이때 처음 확정됨.

**5회 시도(전부 되돌림)**:
1. URDF 상한 전역 1.05→0.3 축소 — 2b-transport/3-return은 좋아졌으나 1-standoff(오른팔)가 새로 클램프되며 더 이른 시점에 낙하
2. `elbow_upper_override`로 2-lift부터만 캡핑 — 진입 순간 꼬이며 악화
3. 캡 시작점을 1b-contact부터로 변경 — 여전히 악화, contact 자체도 깨짐
4. (사용자 지적) "팔꿈치 굽는 방향이 반대인 것 같다" — 그동안 "음수=좋음"이 오차 수렴만으로 판단한 것이었지 실제 3D상 사람처럼 안쪽인지 검증한 적 없었음을 인지
5. **교훈**: 세그먼트마다 필요한 팔꿈치 범위가 다르고, 구간 경계에서 캡을 갑자기 걸면 그 경계 자체가 깨짐 — 단순 파라미터 튜닝으론 못 푸는 구조적 문제.

세션 종료 시점 최고 성공률은 여전히 `084d671`(admittance-88pct, 이송거리 88%).

---

## 3. `084d671` 롤백 후 재출발 — `retry-from-88pct` 브랜치 (2026-07-15~16)

사용자가 `084d671` 자체가 이미 이송거리 93%+ 안정적이고 마지막 착지 구간 하나만 문제였음을 확인 → 순수 진단 로깅 3개만 가진 새 브랜치로 재출발.

### 3.1 deltaY 편류 + F_d 스윕 (5회) — 전부 원복

K=0 순수 적분기 이론(`Md·deltaY_ddot+Da·deltaY_dot=F_ext(deltaY)-F_d` 평형조건은 F_ext(deltaY*)=F_d뿐)을 세우고 리밋(60/35/28mm)과 F_d(10/8/6.5N)를 스윕. 어느 조합도 단조 개선이 아니었고, 최종적으로 **10N/30mm(원본)으로 복귀**.

### 3.2 전채널 종합진단(10개 로그 교차분석) — S/A/B/C 티어

- **S급**: `L_shoulder_roll`이 관절설정과 무관하게 10개 런 전부에서 transport 27~37% 지점에 IK 위치리밋에 부딪힘 (pinocchio Jacobian으로 이 관절이 L_EE 속도의 99.6%를 스퀴즈 방향에 기여함을 수치 검증)
- **A급**: F_d=10N이 지속가능력(6~8N peak)보다 높아 deltaY가 위치+속도 이중 리밋 포화
- **C급(강등)**: "PD 토크 89~100% 포화"로 보였던 것은 실은 `Head_pitch_joint`가 균일 클램프에 고정된 부산물 — 팔/그립과 무관할 가능성

### 3.3 S급(L_shoulder_roll) 해결 시도 2회 — 모두 실패

Null-space 관절한계회피(Liegeois 1977) 항을 offline에서 margin 4배 개선 검증 후 구현.
- **1차(전구간 적용)**: 관절한계는 완전 해결됐으나 **물체가 완전히 바닥 낙하** — 접촉력 붕괴(F_L mean=-0.32N)
- **2차(2b-transport만 한정)**: 여전히 실패, R_elbow margin이 정확히 0으로 바뀜(부하만 이동)
- **근본 원인**: `SolveIK_Position`은 위치만 태스크(`J.topRows<3>()`)로 잡고 방향은 전혀 제약 안 함 — null-space가 관절을 재배치하면 손끝 위치는 유지되지만 손 방향은 부수효과로 회전됨. 이 로봇의 그립은 순수 마찰(패드-물체 정렬)에 의존해 방향에 매우 민감 → 즉시 그립 풀림.

### 3.4 손목 관절 + 방향추종 IK 추가 후 재시도 — 5전 5패로 확정 (커밋 `20717ed`~`a44649b`)

§3.3의 결론(null-space는 방향 미제약 때문에 실패)을 근거로, `SolveIK_Position`에 방향추종 항(손 로컬 Y축이 world Y를 향하도록)을 추가하고 DoF를 11→13으로 확장(wrist_yaw 추가, IK에선 배제). 그 위에서 null-space 3차 재시도 → 여전히 실패.

**정밀 계산으로 확정된 근본 원인** (커밋 `a19fc38`): 13DoF 중 Head(2)와 양쪽 손목(2, 의도적 배제)을 빼면 실질 가용 관절은 **9개**. 현재 태스크(위치6+방향6)를 9개 관절으로 푸는 데 pinocchio rank=9(200회 랜덤자세 전부 동일) — **null-space 여유가 정확히 0**. 손목을 다 풀어 11개를 써도 이 태스크의 진짜 최대 rank가 11이라(1차원 내재 중복) 여전히 여유 0. 한쪽 팔의 방향추종을 빼야만(rank 9→8) 여유 1이 생김을 확인했으나, 실제 적용 시 L_shoulder_roll은 풀렸지만 L_elbow가 새 병목으로 고정되고 방향오차가 재발(whack-a-mole) — **관절한계회피 계열 접근이 5전 5패로 확정**.

**결론**: "이 로봇은 양팔 위치+방향 동시 태스크에 실질 가용 관절이 9개뿐이고, 손목을 다 풀어도 태스크 자체의 필요 rank(11)를 못 넘어 여유가 안 생기는 구조" — 재분배 방식으로는 근본적으로 못 푼다는 게 5번의 시도로 확인됨.

### 3.5 오프라인 pinocchio로 결정적 확정 (커밋 `581490b`, §9.13) — 방향추종이 접근 자체를 차단

grasp_offset을 0.010~0.100 전 범위로 스윕해도 L팔이 물체 표면에 **기구학적으로 닿을 수 없음**을 확인(가장 공격적인 0.010에서도 13.2mm 못 미침). orient_weight를 0.2→0으로 낮추면 L_shoulder_roll margin이 0.0000→0.0941로 회복되고 손이 표면을 실제로 넘어섬. 즉 방향추종 IK 항이 위치 태스크와 같은 관절을 두고 경쟁하다, 위치가 물체에 닿기도 전에 막혀버리는 구조임을 확정.

### 3.6 국소 완화로 성공률 개선 (커밋 `77faaf1`~`3d6888e`) — 66.7%까지

전역 orient_weight를 낮추는 대신 좌우 분리(`orient_weight_L/R`) + 구간별로 다르게 적용:
- 1b-contact만 `orient_weight_L=0.08` (커밋 `77faaf1`): 접촉력 3배 개선(1.5N→4.40N), 하지만 2-lift에서 여전히 L_elbow 100% 고정되며 추락
- 온라인 2-lift/transport까지 `ONLINE_ORIENT_WEIGHT_L=0.12` 확장 (커밋 `96750e1`): 낙하 0회, 25.5cm(당시 최고 기록)
- 재현성 확인(n=3): 33.3%→2-lift `v_des` 0.1→0.08 감속 병행 → **66.7%(n=3)**로 개선
- `orient_weight_R=0.15`도 시도했으나 완전성공 0/3(도달거리는 더 좋음, 0.107m) vs R=0.2(66.7% 완전성공) — 트레이드오프만 확인, 안전한 R=0.2로 최종 확정

**세션 최종 코드 상태**: `orient_weight_L=0.08`(1b-contact 한정) + `ONLINE_ORIENT_WEIGHT_L=0.12`/R=0.2(온라인) + 2-lift `v_des=0.08` + `LIFT_HEIGHT=0.15` + `grasp_offset_L=0.030`/`R=0.040`.

### 3.7 팔꿈치 "닭날개" 시드 문제 발견 및 최종 해결 (2026-07-16, `SESSION_20260716` + 커밋 `61facd5`~`9b83fd6`)

사용자가 직접 Gazebo 실행 후 "박스를 잡자마자 바로 떨어뜨린다"는 새로운(방향과 다른) 문제 보고, 영상 분석: 팔꿈치가 아래가 아니라 하늘로 솟구치는 **역관절 자세("닭날개")**로 수렴 → 시작하자마자 관절한계 도달.

**조치**: 방향추종 IK 완전 제거하고 084d671 시절 순수 위치(6D) DLS로 복귀(§9.28 — "9개 활성 관절로 11차원 태스크를 풀면 null-space가 구조적으로 0"이라는 §3.4의 결론을 여기서도 재확인, 방향추종을 빼서 회피). pinocchio로 elbow-down 유지하는 spawn-safe 시드(400개 무작위 시드 중 margin≥0.36rad인 값)를 찾아 `dual_arm_initp`/`gazebo.launch` spawn `-J`에 하드코딩.

**진짜 원인(§9.29, 커밋 `3a81611`)**: 시드 하드코딩 후에도 왼팔만 여전히 닭날개 — `rostopic echo`로 직접 확인한 결과 스폰 직후 관절값이 전부 0으로, `-J` 플래그가 반영이 안 되고 있었음. 범인은 **`dual_arm_jointp_trajectory = MatrixXd::Zero(1,DoF)`** — 이 매트릭스가 컨트롤 루프의 "궤적 없을 때 마지막 자세 유지" 폴백 로직이 매 tick 참조하는 대상이었는데, 0으로 초기화돼 있어 스폰 직후부터 계속 q=0으로 되돌리고 있었음. 기본값을 시드값으로 교체 → **2-lift 구간 완전히 깨끗해짐**(관절 margin 0.48~1.34rad, 관절한계 문제 완전 해소). 이후 남은 문제는 transport 중 서서히 슬립(joint margin과 무관한 순수 힘/마찰 이슈)으로 성격이 바뀜 — 원래의 P0 어드미턴스 튜닝 영역으로 복귀.

---

## 4. 이번 세션 (`admittance-control` 브랜치) — 최종 완성까지

`friend_ws_88pct`에 `084d671`(admittance-88pct) 기준 git worktree를 새로 만들어 작업(§3의 `retry-from-88pct` 작업은 전혀 건드리지 않음).

### 4.1 그립 패드 원판화 + 접촉 강성 튜닝 (커밋 `f12423c`, `b7592f1`)

사각 패드+L자턱 조합(§1.5)에 `<visual>`이 없어 Gazebo에서 안 보이는 문제 발견 → 원형 평면 패드(cylinder)로 통합 교체, rpy는 fk_pad_calc.cpp로 실제 grasp 파이프라인을 재현해 dot=1.0으로 정렬. F/T 센서 실측(Canvas 차트 아티팩트로 시각화)으로 grasp_to_place 구간 평균 힘이 목표 10N의 22%(L)/42%(R)에 불과함을 확인 — 마찰계수(μ=1.2)는 정역학 계산상 필요치(≈0.098)의 12배 여유가 있어 원인이 아니었음. URDF/SDF 어디에도 `<contact>` kp/kd가 명시된 적이 없어 Gazebo 기본값(kp=1e12, kd=1.0 — 사실상 강체)을 그대로 쓰고 있었다는 걸 발견, kp→1e5/kd→100.0으로 재설정.

### 4.2 IK에 null-space q_pref(elbow-down+out) 투영 추가 (커밋 `d55f6df`) — 허리 충돌 문제 최종 해결

팀원(`jys` 브랜치)이 동일한 null-space q_pref 접근을 **양팔 독립(decoupled) IK** 구조 위에서 먼저 시도 → waist/head 고정 + 팔당 4관절(wrist 제외)로 6D 태스크를 풀다 보니 관절 수=태스크 차원이 같아 null-space 여유가 구조적으로 0(§3.4와 동일한 원인 패턴이 다른 브랜치에서 재현된 것). waist를 추가로 포함시키려 해도 waist가 양팔 공유 관절이라 왼팔/오른팔이 원하는 회전 방향이 반대로 나와 평균 내면 상쇄되는 새 문제 발생.

**해결**: `admittance-control`의 `SolveIK_Position`은 애초부터 waist 포함 9개 관절으로 양팔을 **하나의 6D 스택 태스크로 동시에 푸는 coupled 구조**라, waist가 공유 변수가 아니라 태스크에 자연스럽게 녹아있는 컬럼 — jys가 겪은 충돌 자체가 없음. pinocchio로 재검증한 rank=6(항상 유지, null-space=3, jys 최선의 경우인 1보다 훨씬 넉넉)을 확인 후, jys가 검증한 q_pref(elbow-down=-0.45, shoulder-roll 바깥벌림=±0.55)를 이 coupled 구조로 이식. elbow 부호는 `retry-from-88pct`에서 이미 400개 샘플 탐색+Gazebo 검증으로 확정한 값(L_elbow=-0.496, 동일 joint 정의)과 방향이 일치함을 확인 후 사용. Head_yaw/Head_pitch는 null-space 기여분을 명시적으로 마스킹해 원치 않게 끌려가지 않도록 처리. posture_gain은 0.35(jys 원값)→0.1로 낮춰 위치 잔차 6.5mm까지 개선.

**결과**: 사용자가 Gazebo로 직접 검증 — 허리 충돌 없이 팔꿈치가 사람처럼 자연스럽게 굽혀지며 목표까지 도달.

### 4.3 그립 패드 최종 정밀 튜닝 (커밋 `18afd31`, `5ce5b51`)

null-space 도입으로 파지 시점 실제 손 자세가 바뀌면서 패드 정렬도 다시 어긋남 → recompute_pad_rpy.py로 재계산(dot=1.0 재검증, L이 더 크게 바뀜=roll 0.47rad 차이, 사용자가 왼쪽만 눈에 띄게 어긋난 것으로 관찰한 것과 일치).

이후 사용자 피드백 기반 반복 튜닝:
1. 왼손 패드가 손목 mesh에 파묻힘 → 오프셋 축소(과함) → 다시 확대 → 오른손과 매그니튜드만 맞춤(방향은 유지, 프레임이 완전 거울대칭이 아니라서)
2. 시각화 구(place_indicator, 이송목표 마커 — L_EE/R_EE와는 무관한 별개 SDF 오브젝트)와 혼동된 관찰 → 실제로는 손목 구체 자체가 물체에 너무 붙어 목표점이 처지는 문제로 재정의
3. **최종**: 손목 구체 중심→물체중심 방향에서 월드 Z성분을 0으로 강제(손목 구체 중앙 높이 유지)한 뒤 손목 구체 실측 반지름(0.0325m)만큼 나간 지점을 기하학적 탄젠트 접점으로 계산 → origin xyz로 반영 (compute_tangent_offset.py)
4. yaw만 최종 미세조정: 왼쪽 +7.5°/오른쪽 -7.5°(대칭) — 패드 평면이 물체 옆면과 평행하게 정렬

**사용자 최종 확인**: "드디어 손목 구체랑 패드가 붕 뜨는 것 없이 기하학적으로 예쁘게 찰떡 밀착되었어."

### 4.4 이 세션의 커밋 스펀

```
f12423c feat: 그립 패드를 사각+L자턱 조합에서 원형 평면 패드로 교체
b7592f1 tune: 그립 패드 접촉 강성/댐핑을 Gazebo ODE 기본값에서 낮춰 재설정
d55f6df feat: IK에 null-space q_pref(elbow-down+out) 투영 추가
18afd31 fix: null-space IK 도입에 맞춰 그립 패드 rpy 재정렬
5ce5b51 fix: 그립 패드 마운팅을 손목 구체 탄젠트 접점 기준으로 재계산
```
(모두 `admittance-control` 브랜치, GitHub push 완료)

---

## 5. 핵심 교훈 정리

1. **"관절한계에 걸린다"는 증상은 표면적 결과일 뿐, 진짜 원인은 대부분 "태스크 차원 vs 실질 가용 관절 수"의 자유도 예산 문제였다.** 9개 활성 관절으로 6D(위치만) 태스크를 풀면 여유 3, 여기에 방향(6D)까지 더하면 정확히 0 — 이 rank 계산 하나가 §2.5(팔꿈치 5회 실패), §3.3~3.4(null-space 5전 5패), §4.2(jys의 waist 충돌)를 전부 관통하는 동일한 근본 원인이었다.
2. **파라미터 튜닝으로는 구조적 DOF 부족을 못 이긴다.** 관절한계 상한 축소, elbow_upper_override, null-space 게인 조정 등 "숫자만 바꾸는" 접근은 병목을 다른 관절/구간으로 옮길 뿐 근본 해결이 안 됐다 — 결국 태스크 자체를 줄이거나(방향추종 제거), 구조를 바꿔서(coupled IK) 여유 자유도를 실제로 만들어내는 것만 통했다.
3. **"관절 각도가 수렴했다"와 "실제로 물체를 잡았다"는 다른 이야기다.** 세션 초반 obj_z/force 등 간접 신호로 "성공"을 판단했다가, 실제로는 오른손이 물체를 쳐서 날린 것을 성공으로 오인한 사례가 있었다 — `ik_rotErrL`처럼 이미 로깅되고 있었지만 무시됐던 신호를 다시 보는 것으로 재발견됨. 이후 F/T 센서 실측, dot product 정렬 검증(항상 수치로 1.0 확인) 등 직접 검증을 관례화했다.
4. **텔레메트리(로그)만으로 진단하면 상관관계를 인과로 오인하기 쉽다.** contact gate가 전체 세션 내내 단 한 번도 정상 통과한 적이 없었다는 사실(§3.1 인접)도 한참 뒤에야 발견됐다 — "3초 기다렸지만 확인 못 해서 그냥 진행"이라는 공통 배경 위에서 여러 분석이 이뤄지고 있었다.
5. **작은 기하학적 디테일(그립 패드 방향/위치)이 제어 성패를 좌우했다.** 물체 파지가 순수 마찰 기반일 때는 패드 정렬 오차 하나가 스퀴즈력 비대칭 → 회전 → 낙하로 이어지는 사슬을 여러 번 만들었다 — IK/제어 로직을 아무리 잘 만들어도 접촉 기하학이 안 맞으면 무의미했다.
