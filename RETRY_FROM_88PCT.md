# 084d671(admittance-88pct)에서 재출발 - 개발 기록

**작성일**: 2026-07-15
**브랜치**: `retry-from-88pct` (`admittance-88pct` 태그 = 커밋 `084d671`에서 분기)
**배경**: `diag/full-online-logging` 브랜치(별도 보존, `PRD_dual_arm_v4_grasp_failure_rca.md` 참고)에서
접촉 타이밍/좌우 IK 커플링/Waist 분리 등 여러 층을 깊게 판 결과, 원래 출발점인 `084d671` 자체가
이미 이송거리 93%+ 안정적으로 성공했고(회전 없음, 낙하 없음) 마지막 구간 하나만 문제였다는 걸
사용자가 확인함. 그 넓은 원인들을 되짚는 대신, `084d671` + 순수 진단 로깅만 가진 깨끗한 상태에서
다시 좁게 재분석하기로 함.

가져온 커밋(순수 진단 로깅, 부작용 없음): `bda002c`(온라인 tick 로깅), `6ad05c5`(접촉타이밍 로깅),
`3ffd3ae`(model_states 물체 pose 로깅). 리밋/게인/Waist분리 등 실험적 변경은 전혀 안 가져옴.

---

## 1. 첫 재현(순정 084d671 + 로깅만): 6.6cm 앞에서 정지

고정 목표 `(0.45, 0.15, 1.2)`로 재현 - **낙하 없이 안정적으로 목표 6.6cm 앞(0.4802, 0.0987, 1.2281)까지 도달.**

`admittance_log`로 직접 확인: `deltaYL`이 transport 세그먼트의 36% 지점부터 정확히 `-0.03`(`Y_CMD_MAX_DISP` 하드리밋)에 딱 붙어 남은 94% 구간을 그 상태로 정지(`deltaYR`도 `+0.03`에 고정). 사용자 육안 관찰("왼쪽팔이 좀 더 가지 못하고 가만히 정지해있어")과 정확히 일치.

## 2. 왜 멈추는지 이론 (K=0 순수 적분기)

y(스퀴즈) 힘추종 법칙:
```
Ma·deltaY_ddot + Da·deltaY_dot = F_ext(deltaY) − F_d      (K=0)
```
정지(평형) 조건은 `deltaY_ddot=0, deltaY_dot=0`을 대입하면:
```
F_ext(deltaY*) = F_d
```
K=0이므로 이 조건 말고는 멈출 방법이 없다. **`F_ext(deltaY)`가 실제로 `F_d`에 도달하지 못하고 어딘가에서 꺾이면(포화/감소) 이 방정식엔 해가 없고, deltaY는 소프트웨어 리밋까지 무한정 자란다** - "조금만 더 가면 되는" 상태가 아니라 "애초에 도달 불가능한 목표를 향해 미는" 상태.

K≠0(예 임피던스의 `Kd_imp`)이면 `Ka·deltaY* = F_ext(deltaY*) − F_d`가 되어 F_ext가 F_d에 못 미쳐도 항상 유한한 해가 존재(안정적으로 어딘가에서 멈춤) - 이게 임피던스가 애초에 안정적이었던 이유.

## 3. 리밋 조정 실험 3회

| 리밋 | 결과 | obj_z 낙하 | 목표까지 거리 |
|---|---|---|---|
| 30mm(원본) | 안정, 리밋에 고정 | 없음 | 6.6cm |
| **60mm** | **발산 - 완전 실패** | **있음(바닥 0.05m)** | 1.16m |
| 35mm | 안정, 리밋에 고정 | 없음 | **4.66cm** (개선) |

60mm는 이론과 정확히 일치 - "여유를 더 줘도 F_ext(deltaY)=F_d를 만족하는 지점이 없으면" 그냥 더 멀리까지 밀다가 결국 접촉을 완전히 잃고 발산.

## 4. F_ext(deltaY) 곡선을 기존 로그에서 직접 재구성 (새 Gazebo 실행 불필요)

3개 런의 `admittance_log`에서 `deltaYL_m`을 2mm 단위로 구간화(bin)하고 그 구간의 평균 `F_lpf_L_y_N`을 집계 - **매번 새로 실행하지 않고 이미 가진 로그만으로 F_ext(deltaY) 관계를 직접 관찰**하는 방법.

**결과**:
- **peak(F_max) ≈ 6~8N, deltaY ≈ 15~25mm 부근**에서 발생.
- **28mm 근처부터 힘이 하락하기 시작**(30mm런 28mm구간: 4.8N·표준편차3.8로 이미 흔들림 시작. 35mm런: 28→30→32→34mm에서 6.0→5.0→2.8→2.4N로 뚜렷하게 하락).
- 60mm런에서는 38mm 이후 힘이 거의 0(심지어 음수) - 접촉이 완전히 풀려 손이 허공을 미는 상태.

**결론**: 35mm가 30mm보다 목표에는 더 가까워졌지만(4.66cm), 이미 하락 구간 문턱(28mm대)을 살짝 넘어서 있어 사용자가 "덜 안정적으로 느껴진다"고 느낀 것과 일치 - 완전히 무너지진 않았지만 벼랑 끝에 더 가까운 상태. **peak(15~25mm)까지는 가되 하락 구간(28mm+)에는 안 들어가는 게 진짜 sweet spot으로 추정** - 다음 시도: **28mm**.

## 5. 28mm 재시험 결과: 예측과 반대로 최악

28mm 재시험 결과, 목표까지 75.3cm(물체가 obj_z 1.22→0.46로 사실상 그립을 놓치고 추락) - 30/35mm보다 훨씬 나쁨.
시계열 직접 분석: transport 49% 지점에서 deltaYL/R이 리밋(28mm)에 붙은 채 F_L_y가 4.2N→7.6N→5.1N으로
진동, 63% 구간을 리밋에 물려있다가 그립을 완전히 놓침. 곡선 재구성 예측("하락 문턱 직전이라 안전할 것")과
정반대 - **정적 F_ext(deltaY) 곡선만으로는 어디서 멈출지 예측할 수 없다**(접촉엔 이력/속도 의존성이 있어
동일 deltaY라도 상황에 따라 다른 힘이 나옴). 30mm로 원복.

## 6. F_d 스윕(10N→6.5N→8.0N): 단조 관계 아님, F_d만으로는 예측 불가

F_d를 낮추면 리밋에 덜 붙을 것이라는 가설로 6.5N, 8.0N을 순차 시험(30mm 리밋은 유지):

| F_d | 리밋 붙잡힘 | 힘 std | 목표까지 거리 |
|---|---|---|---|
| 10N | 59.9% | 3.85N | 6.6~6.8cm |
| 8.0N | 45.8% | 4.78N | **16.4cm (최악)** |
| 6.5N | 5.3% | 5.67N | 9.66cm |

6.5N은 의도대로 리밋에서 벗어났지만(5.3%) 오히려 힘 std가 늘고(3.85→5.67N) y축 오차도 커짐(5.4→9.4cm) -
30mm 하드 리밋이 K=0 발진을 억지로 눌러주는 우발적 댐퍼였고, F_d를 낮추면 얕은 평형점(스퀴즈 깊이 부족)이
되어 마찰이 줄어드는 트레이드오프가 있음. 8.0N은 10N/6.5N 사이 중간값을 예상했으나 실제로는 셋 중
최악(16.4cm) - **F_d와 결과 사이에 단조 관계가 없고, 조건당 1회 실행이라 Gazebo 접촉 시뮬레이션의
실행별 노이즈가 파라미터 효과보다 클 가능성**이 제기됨. F_d=10N/30mm로 복귀.

## 7. 재현성 검증 (F_d=10N/30mm 2회 반복)

같은 설정(10N/30mm)으로 2회 반복 실행: 6.84cm vs 6.49cm (편차 ≈5%) - **지금까지의 30/35/28mm, 10/8/6.5N
비교가 순수 노이즈로 설명되기엔 편차가 너무 작음**. 즉 6번 절의 관측(28mm/8.0N이 유독 나쁨)은 진짜 파라미터
효과일 가능성이 높고, 노이즈만으로 뒤집힐 정도는 아님. 다만 F_d/리밋 자체가 결과를 결정하는 유일한 변수는
아니라는 게 이어지는 8절에서 드러남.

## 8. 종합 진단: 전 채널 스윕 결과 및 의심 항목 티어리스트

사용자 요청으로 코드/실행 변경 없이 기존 로그(admittance_log, contact_log) 전 채널과 URDF/코드를 대조해
가능한 모든 의심 지점을 수치로 검증. `072449`(30mm/10N 최초), `072841`(60mm), `073531/073653/073825/073956`
(35mm 4회), `074851`(28mm), `080013`(6.5N), `080441`(8.0N), `080730`(10N 반복) - 10개 런을 교차 비교.

### S급 - 최우선 (config-independent, 수치 검증됨)

**L_shoulder_roll 관절이 IK 위치리밋에 실제로 부딪힌다 - 어드미턴스 튜닝과 무관하게 모든 설정에서 공통 발생.**

`margin_L_shoulder_roll_joint_rad`(관절 여유, `min(q-lower, upper-q)`)가 0에 도달하는 비율과 시점을
10개 런 전부에서 확인:

| 런 | 설정 | margin=0 비율 | 최초 도달 시점 |
|---|---|---|---|
| 072449 | 30mm/10N | 16.7% | 34.5% |
| 072841 | 60mm/10N(발산) | 47.9% | 35.9% |
| 073531 | 35mm/10N | 18.3% | 34.3% |
| 073653 | 35mm/10N | 58.1% | 27.0% |
| 073825 | 35mm/10N | 27.0% | 34.7% |
| 073956 | 35mm/10N | 24.3% | 34.2% |
| 074851 | 28mm/10N | 15.4% | 35.3% |
| 080013 | 30mm/6.5N | 4.2% | 37.2% |
| 080441 | 30mm/8.0N | 16.9% | 34.0% |
| 080730 | 30mm/10N | 18.5% | 32.8% |

**최초 도달 시점이 27~37%로 설정과 무관하게 항상 비슷함** - deltaY 리밋 조정(28~60mm)이나 F_d 조정
(6.5~10N)으로 이 값이 거의 안 바뀜. 심지어 deltaY가 리밋에 거의 안 붙는 6.5N 런(080013)도 이 관절
리밋에는 37.2%에 도달함 - **즉 지금까지 튜닝해온 deltaY/F_d와는 별개의, 더 상위(기구학적) 제약**으로 보임.

기구학적 인과관계도 수치로 확인(pinocchio Jacobian, neutral pose): L_shoulder_roll_joint가 L_EE
선속도에 기여하는 방향은 world `[0, 0.996, 0.087]`(정규화) - **99.6%가 world Y(스퀴즈 방향)**. 즉 이
관절이 막히면 스퀴즈 방향 이동 자체가 물리적으로 막힌다. 코드 확인(`dual_arm_function.cpp:242-243`):
`SolveIK_Position`은 DLS 반복마다 `q(i) = clamp(q(i), lower, upper)`로 사후 클램프만 하고, 리밋회피를
최적화 자체에 반영하지 않음 - 한계에 도달하면 그 방향으로는 더 못 가면서 명령은 계속 그쪽을 요구하는
상태가 됨. **F_d/deltaY 리밋을 아무리 조정해도, 이 관절이 27~37% 지점에서 막히는 한 한계가 있을 수 있음.**
단, 6.5N(deltaY 거의 안 막힘)도 9.66cm로 부족했던 걸 보면 이것만으로 전체 오차가 설명되는지는 불확실 -
**단독으로 "이것만 고치면 해결"이라 단정할 근거는 아직 부족**하나, 가장 유력한 다음 조사 대상.

### A급 - 기존에 확립됐고 이번에 보강된 것

- **F_d=10N이 지속가능 스퀴즈력(peak 6~8N)보다 높음**(§2, §4): 이번에 deltaY *속도*도 확인 - 30mm/10N
  런에서 deltaY 속도가 `Y_CMD_VEL_LIMIT`(0.03m/s)에 도달하는 비율이 31.6%(신규 확인). 위치리밋 포화(60%)에
  이어 속도리밋 포화까지 겹쳐, K=0 미도달 평형 가설을 3중으로 뒷받침.
- **오른팔 스퀴즈력이 왼팔보다 불안정**: F_R_y std 6.3~7.3N vs F_L_y std 4.0~4.3N(반복 2회 모두 동일 경향).
  부호변화 횟수는 반대로 R이 더 적음(14 vs 49) - R은 저빈도·큰 진폭, L은 고빈도·작은 진폭으로 다른 진동
  양상. URDF 주석(`dual_arm.xacro` L_EE/R_EE 파지패드 정렬 보정 기록: L_EE 40.8°, R_EE 17.2° 어긋남,
  이미 부분 보정 완료·전체 형상 재설계는 P2로 보류 명시)과 방향이 일치.

### B급 - 실재하나 2차적

- **F/T 원시 신호 노이즈가 큼**: `F_raw - F_lpf` 잔차 표준편차가 L=3.7~4.0N, R=4.1~4.8N - F_d=10N 목표
  대비 매우 큰 노이즈 바닥. 어드미턴스가 반응하는 신호의 상당 부분이 실제 접촉력이 아니라 노이즈일 가능성.
- **물체 y슬립과 좌우 힘 역전이 겹침**: obj_y 급변(상위 1%, 최대 ~5mm/tick) 시점들을 F_L/F_R과 대조하면
  L/R 힘이 큰 폭으로 반대 부호를 보이는 순간과 겹치는 경우가 다수 확인됨(예: 46.4% 지점 F_L=+9.17N,
  F_R=-7.88N일 때 obj_y 2.51mm 점프). 스퀴즈 불균형→슬립 메커니즘과 정합하나 전수 인과 검증은 아님.

### C급 - 의심했지만 재검증 후 기각/보류 (계측 한계로 미확정)

- **PD 토크 포화(`pd_acc_max_abs`≈100)가 10개 런 전부에서 89~100%로 나타나 처음엔 S급 후보로 봤으나,
  재조사 후 강등**: `margin_Head_pitch_joint_rad`가 전체 로그(양쪽 세그먼트 전부)에서 분산 0인 완전
  상수값(0.0000795rad)으로 나타남 - 이는 Head_pitch가 스캔 자세(pitch=30°=0.5236rad, URDF 리밋과 정확히
  일치)에 고정된 채 전혀 움직이지 않고 있다는 뜻. URDF상 Head_pitch 액추에이터 용량은 겨우 effort=10Nm인데
  코드의 PD 토크 소프트클램프는 관절 구분 없이 균일하게 ±100 - 실제로는 무게가 훨씬 큰 팔 관절이 아니라
  **머리가 자기 용량(10Nm) 대비 과도한 토크를 계속 요구하며 포화의 주범일 가능성이 높음** - 팔/그립
  문제와 무관한 별개 이슈로 추정. 관절별 `PD_acc` 로그가 없어(현재는 전 관절 합산 norm/max만 기록) 확정
  불가 - **다음에 코드를 바꾼다면 최우선으로 관절별 PD_acc 로깅을 추가해 이 가설부터 확정할 것을 권고**.
- **접촉 시작 시각 L/R 비교("차이 0.0ms")는 철회**: `contact_log`를 정밀 재조사한 결과, 접촉 전 구간은
  로그에 거의 기록되지 않고(31.9s 시점 큰 오차 1행 → 46.5s 시점 이미 거의 0으로 점프) 접근~접촉 사이
  과정 자체가 성기게 기록됨 - 이전에 "L/R 접촉이 동시(0.0ms 차이)"라고 본 것은 로그 해상도의 착시일 수
  있어, 현재 로그로는 L/R 접촉 타이밍 비대칭 여부를 신뢰성 있게 판단할 수 없음(계측 공백).
- **측정 공백- 물체 자세(회전) 미기록**: `object_world_pose`(`main.cpp:161`)는 `Vector3d`(x,y,z 위치)만
  담고 있어 회전(roll/pitch/yaw)은 이 브랜치의 어떤 로그에도 전혀 없음. 다른 브랜치(`diag/full-online-logging`)
  탐색에서는 물체 회전이 핵심 문제로 지목된 적이 있는데, 이 브랜치에서는 회전 발생 여부 자체를 확인할
  방법이 없는 상태 - 결론을 내릴 때 이 공백을 감안해야 함.

**종합 판단**: 단독으로 "이것만 고치면 끝"이라 확신할 만큼 압도적인 단일 원인은 아직 못 찾음(S급
L_shoulder_roll도 6.5N 런에서 deltaY 자체는 안 막혔는데 여전히 미달이었다는 반례가 있음). 다음 우선순위는
(1) L_shoulder_roll이 왜 27~37% 지점에서 항상 막히는지(목표 궤적 자체의 요구치 문제인지) 확인, (2)
관절별 PD_acc 로깅 추가로 C급 가설 확정, 순.

## 9. S급(L_shoulder_roll) 원인 조사 및 해결 - 사용자 승인으로 자율 진행

**2026-07-15, 사용자가 자리를 비우며 위임**: "S급부터 조사하고 문제를 해결해봐. 바꿀때마다 커밋과 기록을
남겨두고 노션도 허락할테니까 정리해놔. 토큰이 중간에 소모될 수 있으니 그전까지 기록을 남겨야 해." -
이후 항목은 Gazebo 실행 및 코드 수정을 스스로 수행하며 실시간으로 기록. Notion 동기화도 이 시점부터 허용됨.

### 9.1 원인 정밀화: 궤적 자체 + deltaY가 겹쳐서 L_shoulder_roll을 미는 것 (오프라인 검증, Gazebo 미실행)

rosout.log에서 실제 pick 시점 물체 위치(`obj=[0.468,-0.172,1.191]`)를 확인하고, `main.cpp`의
`addCartesianSegment`/`CartesianLineTrajectory`/`SolveIK_Position` 로직을 pinocchio(Python)로
그대로 재현(Gazebo 미실행, offline만). 스폰 초기자세는 `gazebo.launch`의 `-J ... 0`(전관절 0)과 동일.

**핵심 확인**: transport 구간(`liftL→transportL`, `liftR→transportR`)만 IK로 풀어도(deltaY=0)
L_shoulder_roll margin이 tau=0.25(정확히 관측된 27~37% 구간과 일치) 부근에서 **0.073rad까지
내려갔다가 다시 회복**하는 "골짜기" 모양을 그림 - 실제 로그에서 본 "이 지점에서 막힘"과 정확히
같은 시점. deltaYL=-30mm(관측된 최대 스퀴즈)를 이 구간 내내 추가로 얹으면 margin 최소값이
0.058rad까지 더 내려감 - **스퀴즈(deltaY, A급)가 이미 궤적 자체로 인해 빠듯한 지점(S급)을 밀어서
넘기는 구조**임을 확인. 즉 S급과 A급은 독립된 두 문제가 아니라 하나로 결합된 메커니즘.

**왜 궤적이 이 관절만 쓰는지**: 이 골짜기 지점에서 L_EE의 world-Y 속도에 대한 관절별 기여도를
Jacobian으로 직접 확인: `Waist=+0.466, L_shoulder_pitch=+0.157, L_shoulder_roll=+0.323,
L_shoulder_yaw=-0.140, L_elbow=+0.208`. L_shoulder_pitch/L_shoulder_yaw는 Y방향 지렛대가 있는데도
margin이 1rad대(거의 원점)로 사실상 안 쓰이고 있었음 - DLS가 순수 최소노름해만 구해서 "가장 저항이
적은" L_shoulder_roll/L_elbow에만 부하를 몰아준 것. Waist는 R_EE에도 동시에 영향을 줘서(공유 관절)
DLS가 자연히 기피하는 것으로 보임.

### 9.2 해결: SolveIK_Position에 관절한계회피 null-space 항 추가 (커밋 예정)

**대안 비교**: (a) L_shoulder_roll URDF 리밋 확대 - 실제 하드웨어 안전마진일 수 있어 근거 없이 건드리지
않기로 함. (b) 궤적 경로 자체를 우회 - 웨이포인트 재설계가 필요해 침습적. (c) **IK 자체에 null-space
관절한계회피 항 추가(Liegeois 1977)** - 주작업(태스크 공간 수렴)엔 수학적으로 영향 없이(null-space
투영이므로) 여유 있는 관절로 부하만 재분배 - 가장 안전하고 일반적인 해결책으로 선택.

오프라인(pinocchio) 사전 검증: `dq -= k_null * (I - J+J) * grad(joint-center-potential)`을
DLS 업데이트에 추가.

| k_null | L_shoulder_roll 최소 margin | 최종 IK 오차(L) |
|---|---|---|
| 0(기존) | 0.0716 rad | 0.037 mm |
| 0.05 | 0.2726 rad | 0.208 mm |
| 0.10 | 0.2725 rad | 0.416 mm |
| 0.30 | 0.2725 rad | 1.242 mm |

k_null=0.05에서 이미 margin 개선이 포화(더 올려도 그대로)되고 오차만 계속 커져 **k_null=0.05로 채택**.
deltaYL=-30mm를 얹은 상태에서도 동일하게 margin 0.058→0.227rad로 개선 확인.

`dual_arm_function.cpp`의 `SolveIK_Position`을 수정: 매 반복마다 damped pseudo-inverse `Jpinv`를
명시적으로 구성해 `dq_null = -k_null * (I - Jpinv*J) * grad`를 기존 `dq`에 더하고, 이미 수렴했어도
null-space 재분배가 매 tick 조금씩 누적되도록 `iter > 5`를 만족해야 break하도록 변경. 이 함수는
standoff/contact/lift/transport/return 오프라인 계획과 PHASE_GRASP_TO_PLACE 온라인 tick 루프
전부에서 공용으로 쓰이므로, 한 곳만 고치면 전체 구간에 자동 적용됨.

catkin_make 빌드 성공. Gazebo 검증 진행 중 - 결과는 다음 기록에 이어서.


### 9.3 Gazebo 검증 결과: 실패 - 관절한계는 해결됐지만 그립을 완전히 놓침

**결과**: 목표까지 151.6cm, **물체가 바닥까지 완전히 낙하**(obj_z 0.05m) - 지금까지 나온 모든 결과 중
최악. 관절한계회피 자체는 완벽하게 작동함 - L_shoulder_roll margin 최솟값 0.25rad(리밋 도달 0%,
기존엔 16~19%), 전 관절 margin이 0.18rad 이상으로 전부 여유 있음(관절한계 문제는 확실히 해결됨).
그런데 F_L_y가 mean=-0.32N, std=0.54N으로 **사실상 접촉력이 거의 없음**(목표 10N 근처는커녕 0에 수렴) -
deltaY는 여전히 30mm 리밋에 70.2% 붙어있는데 힘이 거의 안 잡히는 걸 보면, 애초에 손이 물체를 제대로
붙잡지도 못한 것으로 보임.

**원인 분석(추정)**: 오프라인 검증은 transport 구간의 "관절한계 margin"과 "최종 IK 수렴오차(sub-mm)"만
확인했지, **1b-contact/2-lift처럼 손의 정밀한 위치가 그립 성패를 가르는 구간**에서 null-space 항이
실제 도달 자세를 어떻게 바꾸는지는 검증하지 않았다. k_null이 매 IK 호출마다 전 관절을 "범위 중심"으로
당기기 때문에(margin_Head_pitch가 스캔각 0.5236rad에서 정확히 관절범위 중심에 해당하는 값으로 바뀐 것도
같은 증거), 접촉을 확립해야 하는 미세한 접근 단계에서 목표 위치를 몇 mm 어긋나게 만들어 애초에 접촉
자체가 부실하게 형성됐을 가능성이 높음 - "최종 오차가 sub-mm만 커진다"는 오프라인 검증은 이 실패를
예측하지 못했다(오프라인 검증은 transport 구간만 봤지 1b-contact/2-lift는 안 봤음 - 검증 범위의 허점).

**조치**: 이 변경은 되돌린다. `SolveIK_Position`을 원래 순수 DLS로 복구하고, 대신 null-space 항을
**PHASE_GRASP_TO_PLACE의 2b-transport 구간에만 국한**해서 적용하는 방향으로 좁혀서 재시도할 계획
(1b-contact/2-lift는 원래 로직 그대로 둬서 그립 확립은 건드리지 않고, 관절한계 문제가 실제로 발생하는
transport 구간에만 좁게 적용). 이 재시도는 `SolveIK_Position`에 매개변수(예: `bool enable_null_space`)를
추가하는 좀 더 침습적인 변경이 필요함 - 다음 커밋에서 시도.


### 9.4 운영 이슈 발견: 프로세스 정리 누락으로 좀비 프로세스 20개+ 누적

null-space 2차 시도(§9.2 재시도, phase-gated 버전)를 Gazebo로 검증하려는 중, 지금까지 세션 내내 써온
정리 명령(`pkill -f "gzserver|gzclient|rosmaster|roscore|roslaunch|dual_arm_main"`)이 roslaunch가
띄우는 자식 프로세스인 `controller_spawner`/`robot_state_publisher`/`rosout`를 놓친다는 것을 발견함 -
세션 시작(02:37)부터 지금(08:59)까지 매 Gazebo 재실행마다 이 3종 프로세스가 안 죽고 쌓여
**20세트 이상**이 계속 실행 중이었다. `roslaunch`는 이미 뜬 마스터가 있으면 새로 안 띄우고 재사용하는
경향이 있어, gzserver/gzclient는 매번 새로 뜨더라도 오래된 `robot_state_publisher`/`controller_spawner`
가 같은 토픽 이름(`/dual_arm/joint_states`, 각 `*_effort_controller/command` 등)에 계속 붙어있었을
가능성이 있음 - **오늘 실행한 테스트 결과 일부(특히 최근 null-space 1차/2차 시도)가 이 오염의 영향을
받았을 수 있어, 신뢰도에 유의해야 함**. `ps aux | grep -E "ros|gz|gazebo" | ... | xargs kill -9`로
포괄적으로 정리한 뒤 재실행하는 것으로 변경, 이후 모든 실행은 이 방식을 씀. 사용자에게: **오늘 세션의
모든 Gazebo 정량 결과(특히 §6 이후)는 이 오염 가능성을 감안해 해석할 필요가 있음** - 시간이 되면
클린 환경에서 핵심 결과(10N/30mm 베이스라인 등) 재검증을 권장.


### 9.5 phase-gated(2b-transport 한정) 버전도 Gazebo 검증 결과 실패

**결과**: 목표까지 128.3cm, 물체 바닥 낙하(obj_z 0.05m) - 1차 시도(전 구간 적용)보다는 덜 나쁘지만
여전히 원본(6.6cm)보다 훨씬 나쁨. L_shoulder_roll margin은 확실히 개선(0.18rad, 리밋 도달 0%)됐지만,
이번엔 **R_elbow_joint margin이 정확히 0.0000**으로 바뀜 - null-space가 shoulder_roll의 부하를
elbow로 떠넘긴 것. obj_z가 물체 낙하 시작 시점(transport 10.8% 지점)을 직접 보면, R_elbow margin이
그 순간 이미 정확히 0.0(트랜스포트 시작하자마자 즉시 한계에 부딪힘)이었다.

**정밀화된 원인**: `SolveIK_Position`은 **위치만** 태스크로 잡고(`J.topRows<3>()`, 방향 3행은 애초에
안 씀) 방향(orientation)은 전혀 제약하지 않는다. null-space 항이 관절 배치를 바꾸면 손끝 *위치*는
그대로 유지되지만 **손 방향은 부수효과로 회전**한다 - 그런데 이 로봇의 그립은 순수 마찰(패드-물체
접촉면 정렬)에 의존하고, 084d671 자체가 패드 rpy를 스퀴즈 방향에 정밀히 맞추는 보정을 거쳐야 했을
만큼 방향에 민감하다(§0 문서 참고, dot=0.757/0.955 정렬 보정 이력). 게다가 null-space를
2b-transport 시작 지점에 **불연속으로 켜서** 첫 tick부터 큰 교정 스텝이 발생 - 이게 순간적으로 손
방향을 크게 흔들어 마찰 그립이 즉시 풀린 것으로 보인다(10.8% 지점에서 즉시 낙하 시작과 일치).

**결론**: 이 null-space 방식(위치만 있는 IK에 대한 관절중심화 potential)은 **이 로봇의 마찰 기반
그립 구조와 근본적으로 상성이 안 맞음** - 방향을 안 보는 IK에 대고 관절 재분배를 하면 그립이 의존하는
방향 정렬이 깨진다. 두 번의 Gazebo 시도(전역/구간한정) 모두로 이 메커니즘이 일관되게 확인됨.

**원복 및 다음 제안**: 이 세션에서는 여기서 null-space 시도를 접고 `dual_arm_function.h/.cpp`,
`main.cpp`를 null-space 추가 이전 상태로 되돌린다(10N/30mm, 순수 DLS, §7에서 재현성 검증된 6.6~6.8cm
베이스라인). 이후 이어서 시도한다면: (1) IK 태스크에 방향(orientation) 항을 추가해 손 방향을 명시적으로
고정한 채 null-space를 적용 - 근본적이지만 다른 구간에도 영향을 줄 수 있어 신중한 검증 필요. (2)
null-space 게인을 0에서 서서히 ramp-up해서 구간 경계 불연속을 없애는 절충안 - 더 안전하지만 S급 문제를
완전히 해결 못 할 수 있음. (3) S급을 포기하고 남은 6.6cm는 다른 방식(예: 궤적 자체의 lift 높이/경로
재설계)으로 접근.

### 9.6 코드 원복 완료

`git revert 8c46b6e 8dae9ea`로 null-space 관련 코드 변경 2건을 모두 되돌렸다(문서 기록은 위 §9.1~9.5에
그대로 보존). `dual_arm_function.h`/`.cpp`, `main.cpp`가 §7에서 재현성 검증된 10N/30mm 순수 DLS
베이스라인(커밋 `ad18007` 시점)과 동일한 상태로 복구됨.
