# CLAUDE.md

## 커밋 컨벤션 (이 저장소 전용 사전 승인)

코드를 수정/변경하고 그 변경이 완료·검증되면, 사용자에게 매번 묻지 않고 바로 `git commit`을 생성한다.
(이 저장소 안에서 코드 수정에 대한 커밋 생성은 사전 승인됨. force-push, reset --hard 등 파괴적 작업이나
원격 push는 별개 — 그건 항상 확인받는다.)

### 메시지 구조 (제목은 git 관례상 한 줄이지만, 본문은 서술형으로 상세히 — 요약 압축 금지)

```
type: 한줄 요약 (git 제목 관례상 필요, 본문을 대체하지 않음)

배경/문제:
무엇이 문제였는지, 왜 지금 이 변경이 필요했는지를 문단으로 서술.

검토한 대안과 선택 이유:
다른 방법을 고려했다면 무엇이었고 왜 채택/기각했는지.

구현 내용:
실제로 무엇을 바꿨는지 파일/함수 단위로 구체적으로. 필요하면 핵심 수식/로직도 요약.

결과/검증:
어떻게 확인했는지 (실측값, 재현 테스트, Gazebo 확인 등), 남은 리스크나 다음 단계가 있으면 명시.
```

- `type`: `feat` / `fix` / `docs` 등 기존 관례 유지
- 기존 커밋(`fc1b15f`, `d1e806b`, `d0cdcf9` 등)의 서술 스타일을 유지하되, 위 4개 항목을 항상 다 채운다 — 한 줄로 압축하지 않는다
- 하나의 논리적 변경 단위마다 커밋을 나눈다. 여러 개 수정을 한 커밋에 묶지 않는다
- `PRD_dual_arm_v3.md` 기반 작업(P0 어드미턴스, P1 팔꿈치, P3 인식, P4 테이블) 진행 시, 항목 내부의 의미 있는 하위 단계마다 커밋한다 (예: P0라면 "admittance state 변수 추가" → "매 tick IK 호출 연결" → "F/T 검증" 등 단계별로)

### Notion 동기화 (사전 승인됨)

개발 일지 페이지: https://app.notion.com/p/39c14d232f0c81ec8c59d3aa69016b52 ("Dual-Arm PRD v3 개발 일지", P0/P1/P3/P4 섹션 구분)

커밋을 생성할 때마다, 같은 내용(배경/문제 → 검토한 대안 → 구현 내용 → 결과/검증)을 해당 항목(P0/P1/P3/P4) 섹션 아래에 `notion-update-page`로 추가한다. 나중에 PPT로 옮기기 쉽도록 이 구조를 유지한다. 커밋 메시지와 Notion 항목은 내용이 같아야 하며, 별도로 사용자에게 매번 확인받지 않고 진행한다(이 저장소 범위에 한함).
