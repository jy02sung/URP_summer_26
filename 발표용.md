# Dual-Arm Vision Pick & Place — 발표자료

> 카메라 인식 → 좌표 이동 → 파지 → 이송 → 복귀까지 하나의 파이프라인으로 구현한
> 11DoF 양팔 로봇(Head 2DoF + 양팔 4DoF×2 + Waist)의 vision pick 시스템 정리.

---

## 1. 목표 시나리오

```
$ rosrun dual_arm dual_arm_command
[TOP] vision
[VISION] 0
Enter transport target position [x y z]: 0.45 0.25 1.1505
```

명령 하나로:
1. 입력한 좌표(이송 목표)에 **초록 받침대 + 보라 구** 스폰
2. Head가 고정 자세로 움직여 **ArUco 마커 자동 인식**
3. 인식된 물체 위치 기준으로 **양팔이 좌우로 벌어져 접근 → 파지**
4. 파지한 채 **들어올림 → 입력 좌표로 이동 → 내려놓음**
5. **원래 자세로 복귀**

---

## 2. 전체 메커니즘 (파이프라인)

```
[좌표 입력: vision 0 x y z]
        │
        ▼
 place_indicator 스폰 (초록 받침대+보라 구, 입력 좌표에 표시/지지용)
        │
        ▼
 Head 자동 스캔 (HEAD_SCAN_YAW=0, HEAD_SCAN_PITCH=0.5236 고정 이동)
        │  0.5s 정지 대기(SETTLE) → ArUco pose 3프레임 연속 1cm 이내 일치 확인(CHECK)
        ▼
 ArUco pose(카메라 프레임) → TF → world 프레임 변환
        │  마커 위치 + 고정 오프셋(0.0505m) = 박스 중심
        ▼
 양팔 목표 계산: objL/R = 물체중심 ± (0, grasp_offset=0.040, 0)  (표면 안쪽 10mm 침투 목표 → 스퀴즈력 유발)
        │
        ▼
 6세그먼트 Cartesian 직선 궤적 (각 세그먼트 = 매 waypoint DLS-IK)
  ① 시작자세 → 파지 상공(안전고도)         [PHASE_APPROACH]
  ② 파지 상공 → 파지 지점(수직 하강)        [PHASE_GRASP_TO_PLACE] ← 접촉 전부터 임피던스 ON
  ③ 파지 지점 → 리프트 높이(수직 상승)       [PHASE_GRASP_TO_PLACE]
  ④ 리프트 높이 → 이송 목표 상공(수평 이동)  [PHASE_GRASP_TO_PLACE]
  ⑤ 이송 목표 상공 → 이송 목표(수직 하강)    [PHASE_GRASP_TO_PLACE]
  ⑥ 이송 목표 → 시작자세 복귀              [PHASE_RETURN]
        │
        ▼
 완료 알림(/dual_arm/TrajectoryDone) + TaskPhase = APPROACH로 리셋
```

**장애물 회피 원칙**: pick이든 place든 "목표 바로 위가 아니면 항상 안전고도(LIFT_HEIGHT=0.10m 위)로만 이동"으로 통일 — 받침대가 안전고도보다 낮기만 하면 목표 위치가 어디든 일반적으로 통함.

---

## 3. 핵심 구현 요소

| 구성요소 | 설명 |
|---|---|
| **11 DoF** | Waist(1) + Head yaw/pitch(2) + 양팔 4×2 (shoulder pitch/roll/yaw, elbow) |
| **IK** | `SolveIK_Position`: 좌우 EE를 6D 스택 태스크로 묶은 DLS(Damped Least Squares), λ=0.1, tol=1e-4, 위치만 제어(방향 자유) |
| **Cartesian 궤적** | `CartesianLineTrajectory`: quintic time-scaling(s-curve)으로 직선 경로 생성, 매 waypoint마다 IK 재계산 |
| **TaskPhase** | `APPROACH(0) / GRASP_TO_PLACE(1) / RETURN(2) / SCAN(3)` — 궤적 행마다 태깅, 재생 중 자동 전환 |
| **임피던스 제어** | `PHASE_GRASP_TO_PLACE` 구간에서만 활성화. 가상 스프링-댐퍼(Md·e¨+Bd·ė+Kd·e = F_ext)로 접촉력을 관절가속도 보정에 반영 → RNEA로 최종 토크 산출. 전 구간 동일한 `Kp/Kd`(관절 PD, `dual_arm_function.h:100-101`)를 쓰며 파지 중 별도의 낮은 게인으로 전환하는 로직은 없음 |
| **F/T 센서** | `gazebo_ros_ft_sensor` 플러그인, `L_EE_joint`/`R_EE_joint`에서 반력 측정 → `/dual_arm/left(right)_ft_sensor` (WrenchStamped) → **10Hz 로우패스 필터**(`LowPassFilter`, `main.cpp` 임피던스 블록) 통과 후 임피던스의 F_ext로 사용 (원시값은 접촉 순간 수십 N대로 수 ms 만에 요동쳐 필터 없이 쓰면 그대로 토크에 증폭됨) |
| **그립 패드+턱** | 손 mesh가 구형(반경~3.25cm)이라 점접촉만 발생 → 스퀴즈 방향 로컬면에 4×4×1cm 평면 패드 + 1cm L자형 턱 추가, 마찰뿐 아니라 기하학적으로도 물체 지지 |

---

## 4. 이번 세션에서 시도한 것들 (타임라인)

| 시도 | 결과 |
|---|---|
| **이송목표 도달범위 사전 검증** (`TRANSPORT_TARGET_X/Y/Z_MIN/MAX`) 추가 — 범위 밖 좌표 즉시 거부 | 도입(커밋 `e3f34a1`) 후, 정상 사용 흐름을 막는다는 판단으로 **제거 요청 → 삭제** |
| **IK 실패 전염 차단** — 세그먼트 끝까지 IK 미수렴 시 다음 세그먼트 시드를 base_seed로 리셋 | 유지. 국소적 방어(그 세그먼트 자체의 실패는 못 막음, 도미노만 차단) |
| **파지 대상 물체를 y=0(로봇 정중앙)으로 이동** — "대칭이라 더 쉬울 것"이라는 가설 | **실패.** pinocchio로 실제 IK를 재현해 시작자세→파지상공 구간을 y값별로 스캔한 결과, `\|y\|<0.15` 전 구간이 양팔이 경로 중간에 팔꿈치 완전신전 한계에 동시에 박혀 못 빠져나오는 **죽은 구간(dead zone)** 이었음 (y=0에서 최종오차 1.09, 전관절 클램핑). 기존 y=-0.15로 원복 |
| **그립 패드(4×4×1cm) 추가** | 성공. mesh 반경(~3.25cm) 대비 1cm 더 튀어나오게 배치해 평면접촉 확보 |
| **L자형 턱(1cm) 추가** | 성공. 마찰(μN)만으론 접촉 예산이 안 늘어 물체 아래 모서리를 기하학적으로도 받치도록 보강 |
| **Gazebo F/T 센서 플러그인 추가** | 성공. `disableFixedJointLumping`+`provideFeedback` 없이는 fixed joint가 URDF→SDF 변환 시 lumping되어 피드백 자체가 안 나온다는 점이 핵심 |
| **"가장 잘 됐던 시점(2시경)"으로 롤백 시도** | git log/reflog/stash/dangling commit까지 조사했으나 **정확한 2AM 스냅샷은 존재하지 않음** 확인 (00:11 `4035789`↔06:41 `e3f34a1` 사이 6.5시간 공백, 커밋 없음). 가장 가까운 `4035789`로 복귀, 이후 필요한 것만 재적용 |
| **place_indicator(초록 받침대+보라 구) 재구현** | `4035789`엔 없던 기능이라(원래 `e3f34a1`에서 처음 추가됐던 것) 다시 구현 |

---

## 5. 파지 슬립(F/T 비대칭) 원인분석 및 수정

이송 중 물체가 손에서 빠져나가 떨어지는 문제를, gazebo를 직접 실행하고 F/T 센서 + 박스 pose를
1kHz로 로깅해 재현/분석했다.

**증상**: 파지 후 리프트~이송 도중 물체가 완전히 빠져나가 바닥으로 자유낙하 (사용자 체감: "양쪽 힘이 달라서 떨어지는 것 같다")

**로그로 확인한 원인**

| 원인 | 근거 |
|---|---|
| 파지 시작 순간부터 좌우 F/T 비대칭 | 파지 순간 L=(−4.2, 14.2, 39.1N) vs R=(−0.1, −0.7, −1.5N) — 한쪽만 순간적으로 세게 파고듦 |
| F/T 원시값이 필터 없이 임피던스 가속도 계산에 직접 사용됨 | 관절속도는 `LowPassFilter`를 거치는데 F/T(`left/right_ft_force`)는 예외적으로 원시값 그대로 `F_ext`에 사용 → 센서 노이즈(수십 N대가 수 ms만에 요동)가 그대로 토크로 증폭되어 접촉이 떨림(chatter) |
| 스퀴즈력이 정적 유지분만 계산됨 | 기존 grasp_offset=4.5mm 침투 × Kd_imp=300N/m ≈ 1.5N/arm. `IMPEDANCE_GRASP.md`에도 "정적으론 여유 있음"만 명시돼 있었고 관성부하는 고려 안 됨 — 실측상 슬립은 정확히 이송 수평속도가 최고조인 구간에서 시작 |
| 슬립 감지/보정 피드백 없음 | 힘이 새기 시작한 뒤(~3초 동안 서서히 높이 하강) 아무 대응 없이 궤적을 그대로 재생하다 완전히 놓침 |

**적용한 수정** (커밋 `fc1b15f`)

1. `left_ft_force`/`right_ft_force`에 기존 `LowPassFilter(10Hz)` 적용 후 `F_ext`로 사용
2. `Kd_imp_left/right`: 300 → 500 N/m
3. `grasp_offset`: 4.5mm → 10mm 침투 (스퀴즈력 정적 기준 ~1.5N/arm → ~5N/arm)
4. `Bd_left/right`: 50 → 65 (Kd_imp 상향에 맞춰 가상 임피던스 임계감쇠 유지, ζ=Bd/(2√(Md·Kd))≈1.0)

**재현 테스트 결과** (동일 이송 목표로 수정 전/후 비교)

| | 수정 전 | 수정 후 |
|---|---|---|
| 리프트 최고 높이 | 1.266m (목표 1.30m 미달) | **1.302m** (목표 달성) |
| 그립 중 최저 높이 | 자유낙하 → 0.05m (바닥) | **1.19994m** (슬립 사실상 0) |
| 최종 결과 | 낙하 | 목표 지점(0.45, 0.15, 1.2) 근접 위치에 **정지 상태(twist=0)로 안착** |

---

## 6. 커밋 히스토리

`jmp` 브랜치(위 4번 항목의 세션 당시)는 이후 `jmp-restore-2am`과 병합 충돌이 나 미해결로 남아있었고,
현재는 `git merge --abort` 후 **`jmp-restore-2am` 브랜치로 전환**해 작업 중이다. 아래 위쪽 3개는
당시 `jmp` 브랜치 기록(참고용 히스토리), 아래쪽 3개가 현재 브랜치(`jmp-restore-2am`)의 실제 조상 커밋이다.

```
e1a6c5b  (jmp, 병합 미해결로 보류) wip: P0 파지 안정화 부분 개선 + P1 방향정렬 IK 버그 수정 + P2 Head 격자 스캔
e3f34a1  (jmp) fix: 이송목표 도달범위 검증, IK 실패 전염 차단, 파지 접근 경로 안전고도 통일
4035789  (jmp, jmp-restore-2am 공통 조상) docs: README 최신 코드 기준으로 전면 수정 (LIFT_HEIGHT, rnea, pick_pedestal 반영)

fc1b15f  (HEAD, jmp-restore-2am) fix: F/T 로우패스 필터 + 스퀴즈 힘 마진 상향으로 파지 슬립 방지
934be61  (jmp-restore-2am) docs: 발표용 자료 추가 (F/T 비대칭 슬립 원인분석 전 체크포인트)
5e893c8  (origin/jmp-restore-2am) feat: 파지 안정화 및 방향정렬 개선 (jmp-restore)
```

- `4035789` — 전날 밤 완성된 "Cartesian 직선 궤적 + 안전높이 + kinematic attach + Head 자동 스캔 + ArUco 파지 파이프라인" 기준 README 최종화 시점. `jmp`/`jmp-restore-2am` 공통 조상.
- `e3f34a1` — (jmp) 이송목표 도달범위 검증, IK 실패 전염 차단, 접근경로 안전고도 통일, 그립 패드/턱, F/T 센서 플러그인, place_indicator 최초 도입.
- `e1a6c5b` — (jmp) 파지 안정화(P0), 방향정렬 IK 버그 수정(P1), Head 격자 스캔(P2) 진행 중(wip).
- `5e893c8` — (jmp-restore-2am) 파지 안정화 및 방향정렬 개선.
- `934be61` — 발표용 자료 최초 커밋 (F/T 슬립 분석 전 체크포인트).
- `fc1b15f` — F/T 로우패스 필터 + Kd_imp/grasp_offset 조정 (5번 항목 내용, 재현 테스트로 슬립 해결 확인).

---

## 7. 알려진 한계 / 남은 과제

- **IK가 위치만 제어, 방향(orientation)은 자유도로 남김** → 그립 패드/턱 배치가 특정 pick 위치(0.45, -0.15)에서 실측한 손 자세에 종속적. pick 위치가 바뀌면 패드 정렬이 어긋날 수 있음 — 근본 해결은 IK에 방향 정렬 태스크 추가.
- **팔꿈치가 사람과 반대 방향으로 꺾이는 자세로 수렴할 수 있음 (미해결, 다음 작업 예정)** — `SolveIK_Position`(`dual_arm_function.cpp:196`)이 순수 DLS로 위치(3D)만 맞추고 팔꿈치 방향을 제어하는 항이 없어, 어깨3+팔꿈치1의 여유자유도(redundancy) 때문에 시드값에 따라 우연히 반대쪽으로 수렴 가능. `L_elbow_joint`/`R_elbow_joint` limit(`-1.9~1.05`)도 0을 가로질러 양쪽 방향을 다 허용해 이를 막아주지 못함. 대안으로 (1) elbow joint limit을 사람처럼 굽혀지는 방향으로 좁히기, (2) DLS에 null-space 보조항(`q̇ = J⁺ẋ + (I−J⁺J)k(q_pref−q)`)을 추가해 팔꿈치를 선호 방향으로 바이어스 — 2번으로 진행 예정.
- **왼팔(L_EE) 정렬(dot~0.77)이 오른팔(R_EE, dot~0.99)보다 덜 완벽** → 왼팔 접촉이 상대적으로 덜 평평할 가능성.
- **이송목표 범위 검증을 제거**했으므로, 도달 불가능한 좌표를 넣으면 다시 "몇 초 헛돌다 원인불명 실패"하는 증상이 재발할 수 있음 (사용자가 유효 범위를 알고 입력하는 것을 전제).
- **시작자세→파지상공 구간의 IK 데드존**은 pick 위치가 고정(0.45,-0.15)일 때만 회피된 것이며, 근본 원인(관절 한계) 자체는 해결되지 않음 — pick 위치를 바꿔야 한다면 동일한 방식(pinocchio 스캔)으로 재검증 필요.
- **좌우 F/T 힘 자체의 실시간 감시/보정 루프는 아직 없음** — 이번 수정(5번)은 노이즈 억제와 정적 마진 확보이며, 슬립을 사전에 감지해 스퀴즈를 능동적으로 늘리는 폐루프는 여전히 미구현.
