# 최종 발표 대본 및 슬라이드 화면 초안 (Google Slides 복붙용)

- **원본**: `PPT_PRD.md`, `DEFENSE_GUIDE.md`, `PROJECT_HISTORY.md` + 실제 소스코드 재스캔
- **⚠️ 시간 배분 수정 안내**: `PPT_PRD.md`의 v2 슬라이드별 시간을 다시 합산해보니 **16.5분**으로
  15분을 넘고 있었습니다(이전 커밋 메시지에서 "15분과 정확히 일치"라고 한 것은 제 계산 오류였습니다).
  슬라이드 4·8·10을 각 2분→1.5분으로 조정해 **정확히 15.0분**으로 맞췄습니다.
- **시각자료 파일 위치**: `docs/presentation_assets/` (이 문서와 같은 저장소 루트 기준)
- **사용법**: 아래 블록을 슬라이드 1장씩 그대로 Google Slides에 옮기면 됩니다. 발표자
  스크립트는 실제로 소리 내어 읽었을 때 배정 시간에 맞도록 문장 길이를 조절했습니다.

---

### [슬라이드 1] 타이틀

* **화면 텍스트**:
  - 11자유도 양팔 로봇: Vision-guided Pick & Place
  - 어드미턴스 제어 · Coupled IK · Null-space Projection
  - (부제) 접촉 힘 제어와 여유자유도 기반 자세 최적화의 통합 구현

* **삽입할 시각 자료**: [사용자 삽입: 로봇 풀샷 또는 최종 파지 성공 장면 스크린샷]

* **코드 매핑 포인트**: (해당 없음 — 타이틀 슬라이드)

* **발표자 스크립트 (대본)** *(0.5분)*:
  > "안녕하십니까. 지금부터 11자유도 양팔 로봇의 Vision 기반 Pick & Place 시스템을
  > 발표하겠습니다. 오늘 발표의 핵심은 이 로봇이 물체를 놓치지 않고, 사람처럼 자연스러운
  > 자세로 잡을 수 있게 만든 세 가지 제어 메커니즘 — 어드미턴스 제어, Coupled IK, 그리고
  > Null-space Projection입니다."

---

### [슬라이드 2] 시스템 개요

* **화면 텍스트**:
  - 로봇 구성: 11 DoF = Waist(1) + Head yaw/pitch(2) + 양팔 4×2 (shoulder pitch/roll/yaw, elbow)
  - 손목(wrist) 관절 없음
  - 제어 방식: Effort(토크) 제어, pinocchio RNEA로 역동역학 계산
  - 파이프라인: Head 스캔 → ArUco 인식 → 접근(Coupled IK) → 파지(어드미턴스+Null-space) → 이송 → 복귀

* **삽입할 시각 자료**: 파이프라인 다이어그램(텍스트 그대로 슬라이드 도형으로 옮기거나, 화살표 흐름으로 재구성)

* **코드 매핑 포인트**:
  - `dual_arm_function.h:38` — `const int DoF = 11;`
  - `dual_arm_function.h:39` 주석 — 관절 순서 `0:Waist 1:Head_yaw 2:Head_pitch 3~6:L arm 7~10:R arm`
  - `main.cpp` — `enum TaskPhase` 및 `dual_arm_phase_trajectory` 기반 파이프라인 단계 전환

* **발표자 스크립트 (대본)** *(1분)*:
  > "이 로봇은 총 11개의 자유도를 가집니다. 허리 1개, 목(Head) 2개, 그리고 양팔에 각각
  > 어깨 피치·롤·요와 팔꿈치까지 4개씩 — 손목 관절은 없습니다. 제어는 위치가 아니라
  > 토크(Effort) 레벨에서 이루어지고, pinocchio 라이브러리의 RNEA로 매 tick 실제
  > 역동역학을 계산해 최종 토크를 산출합니다. 전체 동작은 Head가 자동으로 스캔해
  > ArUco 마커로 물체를 인식하고, Coupled IK로 접근한 뒤, 어드미턴스와 Null-space
  > Projection이 동시에 작동하는 구간에서 파지·이송·복귀까지 이어지는 파이프라인입니다."

---

### [슬라이드 3] 이 발표의 구조 — 다룰 메커니즘 3가지

* **화면 텍스트**:
  - ① 접촉을 어떻게 유지하는가 → **어드미턴스 제어** (힘 레벨)
  - ② 양팔을 어떻게 동시에 움직이는가 → **Coupled IK** (기구학 레벨)
  - ③ 팔꿈치를 어떻게 예쁘게 두는가 → **Null-space Projection** (여유자유도 레벨)

* **삽입할 시각 자료**: 없음 (텍스트 슬라이드)

* **코드 매핑 포인트**: (해당 없음 — 로드맵 슬라이드)

* **발표자 스크립트 (대본)** *(0.5분)*:
  > "오늘은 세 가지 층위의 문제를 순서대로 다룹니다. 첫째, 손이 물체에 닿았을 때 힘을
  > 어떻게 제어하는가. 둘째, 양팔이 하나의 허리를 공유하는 상황에서 어떻게 동시에
  > 움직이게 하는가. 셋째, 여러 자세가 가능한 상황에서 어떻게 사람처럼 자연스러운 자세를
  > 고르게 하는가 — 이 순서로 진행하겠습니다."

---

## PART A — 어드미턴스 제어 (힘 레벨)

### [슬라이드 4] 지배방정식과 물리적 직관

* **화면 텍스트**:
  ```
  M ë + D ė + K e = F_ext        (e = x_actual − x_desired)

  M : 접촉 반응이 얼마나 "묵직한지" (가상 질량)
  D : 얼마나 "부드럽게" 멈추는지 (가상 댐핑)
  K : 목표에서 벗어나면 얼마나 "강하게" 되돌아오는지 (가상 강성)

  임피던스: 오차 → 힘(토크)   [단일 루프, 이 로봇도 실제 성공]
  어드미턴스: 힘 → 위치 기준  [이중 루프]
  ```
  > **핵심**: 어드미턴스를 택한 이유는 "임피던스가 불안정해서"가 아니라, 축마다 다른
  > 제어 법칙(위치추종+순수 힘추종)을 섞고 싶었기 때문

* **삽입할 시각 자료**: (선택) 임피던스/어드미턴스 인과관계 비교 표 — `DEFENSE_GUIDE.md` §1.1 표를 슬라이드 표로 재구성

* **코드 매핑 포인트**: 이 슬라이드는 이론 비교이므로 현재 `admittance-control` 코드에는
  임피던스 구현이 남아있지 않음(과거 `main.cpp:775-829`, 현재는 어드미턴스로 대체됨) — 구두
  설명 시 "과거에는 이랬으나 지금 코드는" 식으로 명확히 구분해서 말할 것

* **발표자 스크립트 (대본)** *(1.5분)*:
  > "먼저 힘 제어의 핵심 방정식입니다. M, D, K로 이루어진 가상 스프링-댐퍼-질량
  > 모델인데, 질량은 접촉 반응이 얼마나 묵직한지, 댐핑은 얼마나 부드럽게 멈추는지,
  > 강성은 목표로 얼마나 강하게 되돌아오는지를 정합니다. 임피던스와 어드미턴스는 이
  > 방정식을 어디에 쓰느냐가 다릅니다 — 임피던스는 오차를 바로 힘으로 바꾸는 단일
  > 루프고, 어드미턴스는 힘을 위치 기준으로 바꿔서 별도의 위치 루프가 따라가는 이중
  > 루프입니다. 저희는 사실 임피던스도 이미 성공적으로 구현했었습니다. 어드미턴스로
  > 전환한 이유는 안정성 문제가 아니라, x와 y축은 계획된 궤적을 그대로 따르고
  > 스퀴즈축만 순수 힘 제어로 하고 싶다는 요구사항 때문이었고, 이걸 궤적 레벨에서
  > 처리하는 어드미턴스 구조가 훨씬 단순하게 표현됐기 때문입니다."

---

### [슬라이드 5] 실제 구현 — 축별 혼합 제어 법칙

* **화면 텍스트**:
  ```
  x, z축 (위치추종):
    ẍ_cmd = ẍ_d − [D(ẋ_cmd−ẋ_d) + K(x_cmd−x_d)] / M

  y축(스퀴즈, 순수 힘추종, K=0):
    ÿ_cmd = [F_ext,y − F_d,y − D·ẏ_cmd] / M      (F_d = ±10N)

  x_cmd → SolveIK_Position() → q_cmd → PD + RNEA → 최종 토크
  ```

* **삽입할 시각 자료**: 없음 (코드/수식 위주 슬라이드)

* **코드 매핑 포인트**:
  - 위치추종 가속도 계산: `main.cpp:777-779` (`for (int k : {0, 2})` 루프)
  - 힘추종(deltaY) 계산: `main.cpp:784-804`
  - IK 호출: `main.cpp:808` `dualarm.SolveIK_Position(...)`
  - PD 제어: `dual_arm_function.cpp:161-185` (`PDController`), `main.cpp:818`
  - 최종 토크(RNEA): `main.cpp:899` `pinocchio::rnea(...)`
  - 게인 값: `dual_arm_function.h:149` `Ma_left={2.0,2.0,2.0}`, `:150` `Da_left={65.0,...}`,
    `:151` `Ka_left={500.0,0.0,500.0}`, `:161-162` `ADMITTANCE_FD_Y_LEFT/RIGHT=±10.0`
  - F/T 필터: `dual_arm_function.h:141` `FT_LPF_CUTOFF_HZ=10.0`, `main.cpp:764-766`

* **발표자 스크립트 (대본)** *(1분)*:
  > "실제 구현에서는 x와 z축엔 이 방정식을 그대로 위치추종에 쓰고, 스퀴즈 방향인
  > y축만 강성을 0으로 고정한 순수 힘추종 법칙을 씁니다. 목표 스퀴즈력은 좌우 각각
  > 10뉴턴입니다. 이렇게 계산된 위치 명령을 매 tick 다시 역기구학으로 풀어서 관절각을
  > 구하고, PD 제어와 RNEA 역동역학을 거쳐 최종 토크로 변환합니다. F/T 센서 원시값은
  > 접촉 순간 노이즈가 심해서 10헤르츠 로우패스 필터를 거친 뒤에만 사용합니다."

---

### [슬라이드 6] [사용자 삽입] 실측 데이터 — F/T 센서 그래프

* **화면 텍스트**: (사용자가 실측 후 작성)

* **삽입할 시각 자료**: [사용자 삽입: 그래프] — 목표 스퀴즈력(±10N) vs 실제 도달 힘 시계열, F/T 원시값 vs LPF 필터 후 비교

* **코드 매핑 포인트**: 슬라이드 5와 동일 (F/T LPF, ADMITTANCE_FD_Y)

* **발표자 스크립트 (대본)** *(1분, 사용자 데이터 확보 후 직접 작성 권장)*:
  > "[사용자 삽입] 실제 측정한 F/T 데이터를 보시면 ___. 목표 10N 대비 실제 도달률은
  > ___%였고, 필터 적용 전후로 노이즈가 ___만큼 줄었습니다."

---

## PART B — Coupled IK (기구학 레벨)

### [슬라이드 7] 문제: 공유 허리 관절

* **화면 텍스트**:
  ```
  양팔을 독립적으로 IK를 풀면:
    왼팔 IK → "허리를 왼쪽으로!"   ─┐
                                    ├─→ 같은 Waist 관절에 상반된 명령
    오른팔 IK → "허리를 오른쪽으로!" ─┘

  → 하나의 공유 관절이므로 근본적으로 모순
  ```

* **삽입할 시각 자료**: `docs/presentation_assets/diagram_slide7_waist_collision.png`

* **코드 매핑 포인트**: 이 문제는 **다른 팀원(`jys` 브랜치)의 독립(decoupled) IK 구조**에서
  실측된 것 — 우리 `admittance-control` 코드에는 이 문제가 애초에 없음(슬라이드 8에서 설명).
  실측 수치: 왼팔이 원하는 waist 회전 -0.029 vs 오른팔이 원하는 waist 회전 +0.029 (평균 시 상쇄)

* **발표자 스크립트 (대본)** *(1분)*:
  > "양팔을 독립적으로 역기구학을 풀면 어떻게 될까요? 이 로봇은 왼팔과 오른팔이 하나의
  > 허리 관절을 공유합니다. 왼팔 솔버는 물체에 다가가려고 허리를 왼쪽으로 틀라고
  > 명령하고, 오른팔 솔버는 동시에 오른쪽으로 틀라고 명령합니다 — 같은 물리적 관절에
  > 상반된 명령이 꽂히는 겁니다. 실제로 이 구조로 실험했을 때 왼팔은 -0.029, 오른팔은
  > +0.029 라디안을 요구해서, 평균을 내면 거의 0으로 상쇄되며 로봇이 정체되는 현상이
  > 실측으로 확인됐습니다."

---

### [슬라이드 8] Stacked Jacobian 수식 유도

* **화면 텍스트**: (수식 이미지로 대체, 화면엔 짧은 헤드라인만)
  - "두 손의 야코비안을 하나로 쌓으면, 허리는 '경쟁'이 아니라 '동시 만족'의 최적해가 된다"

* **삽입할 시각 자료**: `docs/presentation_assets/eq_slide8_stacked_jacobian.png`

* **코드 매핑 포인트**:
  ```cpp
  // dual_arm_function.cpp:257-259
  MatrixXd J(6, model.nv);
  J.topRows<3>()    = JL.topRows<3>();   // 왼손 위치 3행
  J.bottomRows<3>() = JR.topRows<3>();   // 오른손 위치 3행

  // dual_arm_function.cpp:261-263 — DLS 의사역행렬
  MatrixXd JJt = J * J.transpose() + (lambda*lambda) * MatrixXd::Identity(6,6);
  MatrixXd Jpinv = J.transpose() * JJt.ldlt().solve(MatrixXd::Identity(6,6));
  ```
  - λ 값: `dual_arm_function.cpp:212` `const double lambda = 0.1;`
  - 함수 전체: `dual_arm_function.cpp:207-286` `SolveIK_Position`

* **발표자 스크립트 (대본)** *(1.5분)*:
  > "해법은 두 손의 야코비안을 하나로 쌓는 것입니다. 왼손 위치 오차 3개, 오른손 위치
  > 오차 3개를 세로로 쌓아 6행짜리 하나의 자코비안을 만들고, 감쇠 최소자승법으로
  > 한 번에 풉니다. 이렇게 하면 허리 컬럼의 값은 왼손과 오른손 오차를 동시에
  > 최소화하는 단 하나의 최적해로 자동 결정됩니다. 두 개의 경쟁하는 명령이 아니라
  > 하나의 방정식이기 때문에, 충돌이라는 개념 자체가 수학적으로 성립하지 않습니다.
  > 저희 로봇은 이 방식을 처음부터 채택했기 때문에, 방금 보신 허리 충돌 문제를
  > 원천적으로 겪지 않았습니다."

---

### [슬라이드 9] 여유자유도(Redundancy)란?

* **화면 텍스트**:
  ```
  9개 관절 − 6D 태스크(양손 위치) = 3개의 "공짜 자유도"

  ⚠️ 방향(orientation)까지 태스크에 넣으면?
     6D×2=12D 태스크, 관절은 여전히 9개 → 여유자유도 = 9−9 = 0
  ```

* **삽입할 시각 자료**: `docs/presentation_assets/diagram_slide9_redundancy.png`

* **코드 매핑 포인트**:
  - 활성 관절 9개(Waist+양팔 4×2): `dual_arm_function.h:38-39`
  - IK가 위치(3D×2=6D)만 풀고 방향은 안 푸는 것: `dual_arm_function.cpp:246-248`
    (`e.head<3>()`, `e.tail<3>()` — 6행뿐, 방향 행 없음)
  - rank=6 검증은 오프라인 pinocchio 스크립트(레포에 미포함, 세션 중 검증)

* **발표자 스크립트 (대본)** *(1.5분)*:
  > "9개 관절으로 6차원 태스크, 즉 양손 위치만 맞추면 3개의 자유도가 남습니다. 손끝으로
  > 책상 위 한 점을 가리키는 팔의 자세가 무수히 많은 것과 같은 원리입니다. 그런데
  > 만약 손의 방향까지 맞추려고 하면 어떻게 될까요? 태스크 차원이 위치와 방향
  > 합쳐서 12차원이 되는데, 관절은 여전히 9개뿐입니다. pinocchio로 실제 랜덤 자세
  > 수백 번을 검증한 결과 이 경우 여유자유도가 정확히 0이 됩니다. 이게 바로 저희
  > 로봇의 역기구학이 방향은 풀지 않고 위치만 푸는 이유입니다 — 방향까지 풀면 이
  > 남는 자유도 자체가 사라져서, 뒤에서 설명할 자세 유도 기법을 쓸 수가 없습니다."

---

### [슬라이드 10] 수학적 증명 + Null-space 투영 공식

* **화면 텍스트**: (수식 이미지로 대체)
  - "손끝 위치는 단 1mm도 안 흔들면서, 팔꿈치만 원하는 방향으로 유도한다"

* **삽입할 시각 자료**: `docs/presentation_assets/eq_slide10_nullspace.png`

* **코드 매핑 포인트**:
  ```cpp
  // dual_arm_function.cpp:272-276
  MatrixXd N = MatrixXd::Identity(model.nv, model.nv) - Jpinv * J;   // (I - J†J)
  VectorXd postureErr = posture_gain * (q_pref - q);                 // k(q_pref - q)
  postureErr(1) = 0.0;  // Head_yaw는 명시적으로 null-space 기여분 제외
  postureErr(2) = 0.0;  // Head_pitch도 동일
  dq += N * postureErr;
  ```
  - q_pref(elbow-down+shoulder-roll 바깥벌림): `dual_arm_function.cpp:233-235`
  - posture_gain=0.1(0.35→0.1로 튜닝): `dual_arm_function.cpp:223`

* **발표자 스크립트 (대본)** *(1.5분)*:
  > "핵심 증명은 한 줄입니다. J 곱하기 (I 마이너스 J의 의사역행렬 곱하기 J)는
  > 항상 0입니다. 이 말은, 이 투영 행렬에 어떤 값을 넣어도 자코비안을 곱하면
  > 0이 된다는 뜻이고, 곧 손끝 속도에 전혀 영향을 주지 않는다는 뜻입니다.
  > 그래서 여기에 '팔꿈치를 이런 자세로 두고 싶다'는 선호값을 넣으면, 손끝은
  > 그대로 둔 채 남는 자유도 안에서만 자세를 조정할 수 있습니다. 저희는 이
  > 선호값으로 팔꿈치를 아래로, 어깨를 바깥으로 벌리는 값을 넣었고, 이 값의
  > 부호는 이전에 400개 샘플 탐색과 실제 Gazebo 검증으로 이미 확정했던 값과
  > 일치함을 재확인한 뒤 사용했습니다."

---

### [슬라이드 11] [사용자 삽입] 실측 데이터 — Before/After 관절 마진

* **화면 텍스트**: (사용자가 실측 후 작성)

* **삽입할 시각 자료**: [사용자 삽입: 그래프] — Null-space 적용 전(마진 0, 붕괴) vs 적용 후(마진 0.16~0.34rad 유지), posture_gain 스윕에 따른 위치오차 트레이드오프

* **코드 매핑 포인트**: 슬라이드 10과 동일 (`dual_arm_function.cpp:272-276`)

* **발표자 스크립트 (대본)** *(1분, 사용자 데이터 확보 후 직접 작성 권장)*:
  > "[사용자 삽입] 실제로 측정해보니 Null-space 적용 전에는 관절 마진이 ___였고,
  > 적용 후에는 ___로 개선됐습니다. posture_gain을 ___까지 낮췄을 때 위치오차가
  > ___까지 줄었습니다."

---

### [슬라이드 12] [사용자 삽입] 데모 영상

* **화면 텍스트**: (영상 삽입, 텍스트 최소화)

* **삽입할 시각 자료**: [사용자 삽입: 영상]

* **코드 매핑 포인트**: (해당 없음 — 데모 슬라이드)

* **발표자 스크립트 (대본)** *(2분)*:
  > "말씀드린 세 가지 메커니즘이 실제로 어떻게 동작하는지 영상으로 보여드리겠습니다.
  > [영상 재생] 보시는 것처럼 팔꿈치가 관절 한계에 부딪히지 않고, 사람처럼 자연스럽게
  > 바깥으로 벌어진 채로 물체에 접근해서, 목표한 힘으로 스퀴즈를 유지하며 이송까지
  > 완료하는 것을 확인하실 수 있습니다."

---

### [슬라이드 13] 마무리

* **화면 텍스트**:
  ```
  수학(Coupled IK, Null-space)과 물리(접촉강성, 마찰, F/T 필터)를
  함께 맞췄을 때만 실전에서 동작했다.

  Q&A
  ```

* **삽입할 시각 자료**: 없음

* **코드 매핑 포인트**: (해당 없음)

* **발표자 스크립트 (대본)** *(0.5분)*:
  > "정리하면, 이 시스템은 수식으로 증명되는 기구학적 원리 — Coupled IK와 Null-space
  > Projection — 와, 접촉 강성이나 마찰, 센서 필터링 같은 물리적 튜닝이 함께 맞아떨어
  > 졌을 때만 실제로 동작했습니다. 이상으로 발표를 마치겠습니다. 질문 받겠습니다."

---

## 시간 배분 검산

| 슬라이드 | 시간(분) |
|---|---|
| 1 | 0.5 |
| 2 | 1.0 |
| 3 | 0.5 |
| 4 | 1.5 |
| 5 | 1.0 |
| 6 | 1.0 |
| 7 | 1.0 |
| 8 | 1.5 |
| 9 | 1.5 |
| 10 | 1.5 |
| 11 | 1.0 |
| 12 | 2.0 |
| 13 | 0.5 |
| **합계** | **15.0** |

## 시각자료 파일 목록 (이번에 생성)

| 파일 | 슬라이드 | 생성 방식 |
|---|---|---|
| `docs/presentation_assets/diagram_slide7_waist_collision.png` | 7 | matplotlib (개념도) |
| `docs/presentation_assets/eq_slide8_stacked_jacobian.png` | 8 | matplotlib mathtext (수식, 300dpi) |
| `docs/presentation_assets/diagram_slide9_redundancy.png` | 9 | matplotlib (개념도) |
| `docs/presentation_assets/eq_slide10_nullspace.png` | 10 | matplotlib mathtext (수식, 300dpi) |

> LaTeX 미설치 환경이라 matplotlib 내장 mathtext 엔진으로 렌더링했습니다(300dpi, 고해상도).
> 매트릭스 표기는 `\begin{bmatrix}`가 mathtext에서 지원 안 돼 `\binom{}{}`(둥근 괄호 스택)으로
> 대체했습니다 — 의미는 동일하나 대괄호가 아닌 괄호로 보일 수 있습니다.
