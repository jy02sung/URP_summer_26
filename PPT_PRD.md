# PRD: 15분 발표(디펜스) PPT 제작 — v2 (원리/수식/코드 중심)

- **작성일**: 2026-07-17 (v1 대비 방향 전환)
- **원본**: `PROJECT_HISTORY.md`, `DEFENSE_GUIDE.md`
- **제작 도구**: Google Slides (아래 슬라이드 블록을 그대로 복사해 붙여넣는 마크다운 초안 형태)
- **v1 → v2 변경**: 사용자 피드백 반영 — **"성공/실패 히스토리보다 코드가 어떤 원리로
  작동하는지(수식·이론·코드)가 훨씬 중요하다. 히스토리는 적은 비중으로, 원리/메커니즘
  설명이 메인이 되게."** 이에 따라 실패 타임라인(5전5패 등)은 각 메커니즘 슬라이드 안에서
  **"왜 이 설계를 택했는가"를 한두 문장으로 뒷받침하는 근거**로만 축소하고, 슬라이드
  대부분을 수식 유도 → 코드 매핑 → (사용자가 직접 준비할) 실측 데이터로 채운다.

---

## 1. 발표 개요 (v1과 동일)

| 항목 | 내용 |
|---|---|
| 청중 | 심사위원(교수님), 15분 + Q&A |
| **콘텐츠 비중** | **원리/수식/코드 70~80% : 히스토리(맥락) 10% : 데모/결과 10~20%** |
| 톤 | 기술 백서 스타일, 과장 없이 사실 기반 |

## 2. 사용자가 직접 준비하는 것 (이 PRD 범위 밖)

- Gazebo 성공 장면 **녹화 영상**
- 성공 장면 재현하며 측정한 **센서 데이터 표/차트** (F/T, 관절각 등)
- 위 데이터를 담은 **아티팩트 스타일 페이지**(Notion에 올린 것과 유사한 형태)

→ 아래 슬라이드 구조안에는 이 자료들이 들어갈 자리를 **"[사용자 삽입]"**으로 명시해뒀다.

---

## 3. 슬라이드 구조안 (총 13장, 15분) — 마크다운 초안

> 각 블록을 Google Slides에 슬라이드 1장씩으로 옮기면 된다. `##`이 슬라이드 제목,
> 본문이 슬라이드 텍스트, 코드블록은 코드 스니펫 박스로 넣을 것.

---

### [슬라이드 1] 타이틀
```
11자유도 양팔 로봇: Vision-guided Pick & Place
어드미턴스 제어 · Coupled IK · Null-space Projection

(부제) — 접촉 힘 제어와 여유자유도 기반 자세 최적화의 통합 구현
```
**시간**: 0.5분

---

### [슬라이드 2] 시스템 개요 (매우 짧게 — 역사 아님, 순수 스펙 소개)
```
로봇 구성
- 11 DoF = Waist(1) + Head yaw/pitch(2) + 양팔 4×2
  (shoulder pitch/roll/yaw, elbow — 손목 없음)
- 제어 방식: Effort(토크) 제어, pinocchio RNEA로 역동역학 계산
- 파이프라인: Head 스캔 → ArUco 인식 → 접근(Coupled IK)
             → 파지(어드미턴스+Null-space) → 이송 → 복귀
```
**시각자료**: 파이프라인 다이어그램 1개
**시간**: 1분

---

### [슬라이드 3] 이 발표의 구조 — 다룰 메커니즘 3가지
```
이 로봇이 "실제로 물체를 놓치지 않고, 사람처럼 자연스러운 자세로" 잡으려면
서로 다른 층위의 세 가지 제어 문제를 풀어야 했다:

① 접촉을 어떻게 유지하는가       → 어드미턴스 제어 (힘 레벨)
② 양팔을 어떻게 동시에 움직이는가 → Coupled IK (기구학 레벨)
③ 팔꿈치를 어떻게 예쁘게 두는가   → Null-space Projection (여유자유도 레벨)
```
**시간**: 0.5분

---

## PART A — 어드미턴스 제어 (힘 레벨)

### [슬라이드 4] 지배방정식과 물리적 직관
```
M ë + D ė + K e = F_ext        (e = x_actual − x_desired)

M (가상 질량)  : 접촉 반응이 얼마나 "묵직한지"
D (가상 댐핑)  : 진동 없이 얼마나 "부드럽게" 멈추는지
K (가상 강성)  : 목표에서 벗어나면 얼마나 "강하게" 되돌아오는지

임피던스: 오차 → 힘(토크)          [단일 루프, 이 로봇도 실제 성공했음]
어드미턴스: 힘 → 위치 기준          [이중 루프]

→ 이 로봇이 어드미턴스를 택한 이유는 "임피던스가 불안정해서"가 아니라
  x,y(위치추종) + 스퀴즈축(순수 힘추종, K=0)을 축마다 다르게 섞고
  싶다는 요구 때문 — 궤적 레벨 개입이 이걸 훨씬 단순하게 표현한다.
```
**⚠️ 발표 시 강조**: 마지막 문장을 반드시 구두로도 말할 것 (Q&A 방어선)
**시간**: 2분

---

### [슬라이드 5] 실제 구현 — 축별 혼합 제어 법칙
```
x, z축 (위치추종):
  ẍ_cmd = ẍ_d − [D(ẋ_cmd−ẋ_d) + K(x_cmd−x_d)] / M

y축(스퀴즈, 순수 힘추종, K=0):
  ÿ_cmd = [F_ext,y − F_d,y − D·ẏ_cmd] / M       (F_d = ±10N)

x_cmd → SolveIK_Position() → q_cmd → PD + RNEA → 최종 토크
```
```cpp
// dual_arm_function.h
Ma_left[3] = {2.0, 2.0, 2.0};      // 가상 질량 [kg]
Da_left[3] = {65.0, 65.0, 65.0};   // 가상 댐핑
Ka_left[3] = {500.0, 0.0, 500.0};  // y축만 K=0
ADMITTANCE_FD_Y_LEFT = 10.0;       // 목표 스퀴즈력
```
**시간**: 1.5분

---

### [슬라이드 6] [사용자 삽입] 실측 데이터 — F/T 센서 그래프
```
(플레이스홀더 — 사용자가 직접 측정/제작)
- 목표 스퀴즈력(±10N) vs 실제 도달 힘 시계열
- 접촉 순간 F/T 노이즈 원시값 vs 10Hz LPF 필터 후 비교
```
**시간**: 1분

---

## PART B — Coupled IK (기구학 레벨)

### [슬라이드 7] 문제: 공유 허리 관절
```
양팔을 독립적으로 IK를 풀면:

  왼팔 IK → "허리를 왼쪽으로!" ─┐
                                ├─→ 같은 Waist 관절에 상반된 명령
  오른팔 IK → "허리를 오른쪽으로!" ─┘

→ 두 관절이 아니라 "하나의 공유 관절"이므로 근본적으로 모순
```
**시간**: 1분

---

### [슬라이드 8] Stacked Jacobian 수식 유도
```
ẋ_L = J_L q̇ ,  ẋ_R = J_R q̇     (같은 q̇를 공유)

J_stacked = [ J_L ]   ∈ R^(6×9)     (양손 위치 6D, 활성관절 9개)
            [ J_R ]

e = [ t_L − x_L ],   q̇ = J⁺_stacked e ,   J⁺ = Jᵀ(JJᵀ + λ²I)⁻¹
    [ t_R − x_R ]

→ 허리 컬럼의 값은 "왼손 오차 + 오른손 오차를 동시에 최소화하는
   단 하나의 최소자승해"로 자동 결정된다. 충돌이라는 개념 자체가
   수학적으로 성립하지 않는다.
```
```cpp
// dual_arm_function.cpp:257-259
MatrixXd J(6, model.nv);
J.topRows<3>()    = JL.topRows<3>();   // 왼손 위치 3행
J.bottomRows<3>() = JR.topRows<3>();   // 오른손 위치 3행
```
**시간**: 2분

---

## PART C — Null-space Projection (여유자유도 레벨)

### [슬라이드 9] 여유자유도(Redundancy)란?
```
9개 관절 − 6D 태스크(양손 위치) = 3개의 "공짜 자유도"

비유: 손끝으로 책상 위 한 점을 가리키는 팔의 자세는 무수히 많다
     (어깨를 돌리며 팔꿈치를 올렸다 내렸다 해도 손끝은 그대로)

⚠️ 방향(orientation)까지 태스크에 넣으면?
   6D×2=12D 태스크, 관절은 여전히 9개 → 여유자유도 = 9−9 = 0
   → 왜 이 로봇의 IK가 "위치만" 풀고 방향은 안 푸는지의 근거
```
**시간**: 1.5분

---

### [슬라이드 10] 수학적 증명 + Null-space 투영 공식
```
J (I − J⁺J) = JJ⁺J − JJ⁺J = 0

→ (I−J⁺J)는 "자코비안을 곱하면 항상 0이 되는 방향"으로만
   투영하는 연산자 — 여기 뭘 넣어도 손끝은 안 흔들린다.

q̇ = J⁺ẋ + (I − J⁺J) · k · (q_pref − q)
     └─태스크(손 위치)─┘   └──자세 선호(팔꿈치)──┘
```
```cpp
// dual_arm_function.cpp:272-276
MatrixXd N = MatrixXd::Identity(model.nv, model.nv) - Jpinv * J;
VectorXd postureErr = posture_gain * (q_pref - q);
dq += N * postureErr;

// q_pref: elbow-down(-0.45) + shoulder-roll 바깥벌림(±0.55)
```
**시간**: 2분

---

### [슬라이드 11] [사용자 삽입] 실측 데이터 — Before/After 관절 마진
```
(플레이스홀더 — 사용자가 직접 측정/제작)
- Null-space 적용 전: 관절 한계까지 마진 0 (붕괴)
- Null-space 적용 후: 마진 0.16~0.34rad 유지
- posture_gain 스윕(0.02~0.35)에 따른 위치오차 vs 관절마진 트레이드오프
```
**시간**: 1분

---

### [슬라이드 12] [사용자 삽입] 데모 영상
```
(플레이스홀더 — 사용자가 직접 녹화한 성공 장면)
```
**시간**: 2분

---

### [슬라이드 13] 마무리
```
수학(Coupled IK, Null-space)과 물리(접촉강성, 마찰, F/T 필터)를
함께 맞췄을 때만 실전에서 동작했다.

Q&A
```
**시간**: 0.5분

---

## 4. Q&A 대비 (변경 없음, `DEFENSE_GUIDE.md` Part 5 그대로 사용)

발표 슬라이드에는 안 넣고, **발표자 노트/백업 슬라이드**로만 보유:
- Q1: 왜 물체가 튕겨나갔나 → 제어공학적 해결
- Q2: 임피던스 vs 어드미턴스 수학적 차이 + 선택 이유
- Q3: Null-space가 제대로 동작하는지 증명 방법
- Q4~Q6: 방향 미제어 이유 / λ 선택 이유 / 실물 이식 가능성

---

## 5. 남은 결정/작업

1. **[사용자 작업]** 슬라이드 6, 11, 12의 실측 데이터/영상 준비
2. **[제가 도울 수 있는 부분]** 슬라이드 8(Stacked Jacobian)·10(Null-space)의 수식을
   Google Slides에 넣을 이미지(고해상도 LaTeX 렌더링)로 미리 만들어드릴 수 있음 — 필요하면 말씀해주세요
3. **[제가 도울 수 있는 부분]** 슬라이드 7(허리 충돌)·9(여유자유도 비유)의 개념도를
   다이어그램으로 만들어드릴 수 있음
4. 전체 리허설 후 시간 재조정 (현재 배분: A 4.5분 / B 3분 / C 4.5분 / 기타 3분 = 15분)
